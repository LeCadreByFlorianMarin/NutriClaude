---
scope: Architecture v1 système entier — NutriClaude (liste de courses multi-surface, brownfield)
purpose: Colonne d'invariants pour la v1
altitude: initiative→features
updated: 2026-07-23T18:53
---

- (constraint) BROWNFIELD ratifié — Stack : Next 16.2 (App Router, Server Actions), React 19.2, Tailwind 4.2, @supabase/ssr + supabase-js. Supabase = DB Postgres + Auth + (RLS) socle. 'Supabase est un détail, pas l'architecture' (exergue doc d'origine).
- (constraint) BROWNFIELD ratifié — Isolation foyer (NFR-5) = RLS sur 10 tables, toutes politiques ancrées sur current_household_id() lisant profiles.household_id = auth.uid(). Vue grocery_list_by_aisle en security_invoker. C'est SAIN et non contournable — mais rend la RLS incontournable pour toute future API (nœud MCP/SERVICE_KEY).
- (constraint) BROWNFIELD ratifié — Beaucoup de règles métier vivent DÉJÀ en Postgres : resolve_aisle_id (mapping produit + mot-clé), generate_grocery_list_from_menu (agrégation/échelle/rayons), create_household_with_profile, redeem/generate_household_invite. Migration = 1 seul fichier initial, aucune discipline de migration incrémentale établie.
- (constraint) RÉALITÉ à trancher (Lot 0 bloquant) — l'app NE COMPILE PAS, 3 causes indépendantes : (1) Tailwind 4 exige @tailwindcss/postcss + globals.css/config en syntaxe v3, (2) cookies() renvoie une Promise en Next 16 (lib/supabase/server.ts casse à l'exécution), (3) TS 6 refuse baseUrl. + middleware.ts (porte TOUT l'auth gating) déprécié en Next 16 → 'proxy'. Arbitrage : avancer en Tailwind 4 vs redescendre en 3.
- (constraint) ÉTAT hors-ligne actuel = INEXISTANT : 5 pages en force-dynamic (aller-retour serveur par action), aucun realtime/channel, aucun service worker, aucun public/ (ni manifeste ni icône). FR-10/22/27 (propagation) et FR-35 (PWA offline) partent de zéro.
- (constraint) DÉRIVE code vs décisions : auth réelle = email+mot de passe (LoginForm signInWithPassword, signup min 6 car.) alors que la décision d'origine était magic link SANS mot de passe — jamais appliquée. Cocher/décocher CASSÉ (état codé en dur, un acheté ne revient jamais). FR-5 (agrégation doublons) inexistante. FR-4 (rayon auto à l'ajout manuel) non câblé. Aucun error.tsx/not-found.tsx (viole NFR-8).
- (version) Versions observées en package.json (2026-07-23) : next 16.2.4, react 19.2.5, tailwindcss 4.2.4, typescript 6.0.3, @supabase/ssr 0.10.2, supabase-js 2.105.1, eslint 10.3.0. À vérifier/confirmer sur le web avant de les lier comme cibles.
- (decision) PARADIGME HORS-LIGNE = A (Supabase seul, local-first léger). Optimiste-local + outbox IndexedDB + convergence LWW, Realtime pour la propagation. AUCUNE infra en plus (PowerSync/Electric écartés pour respecter NFR-10/11 'zéro serveur, tenir sans entretien' ; domaine trop petit pour justifier un moteur de sync). Binds : tout le modèle de mutation d'état et la forme des données mutables. Prevents : deux surfaces qui divergeraient sur 'comment une action locale devient vérité partagée'.
- (decision) CONVERGENCE = dernier-écrivain-par-champ (LWW) arbitré par un updated_at AUTORITÉ SERVEUR sur chaque ligne mutable ; suppression = pierre tombale (deleted_at), jamais un DELETE dur (sinon une suppression hors-ligne ressuscite l'article au retour d'une autre surface). Binds : le schéma de toute table mutable (grocery_list_items en premier). Prevents : résurrection d'articles supprimés, écrasements silencieux non arbitrés.
- (decision) TOGGLE IDEMPOTENT : status (à acheter/acheté) est une valeur POSÉE, pas un basculement relatif — cocher un article déjà coché ailleurs converge sans conflit (NFR-2). Corrige le bug actuel (toggle relatif codé en dur). Suppression et modif de quantité NE sont PAS idempotentes → passent par tombstone / LWW.
- (decision) OUTBOX = file locale ORDONNÉE d'intentions, rejouée au retour réseau et AU PREMIER PLAN de l'app (pas de Background Sync sur iOS — resync seulement à la réouverture, assumé). Le cache de lecture local peut être évincé par iOS après des semaines : acceptable, Supabase est le magasin durable, on repull à la réouverture. Seul risque = écritures non synchronisées perdues si éviction pendant une absence longue hors-ligne — négligeable à l'échelle d'une sortie courses.
- (decision) AUTORITÉ DES RÈGLES : la résolution de rayon (FR-4), l'agrégation des doublons (FR-5) et la mise à l'échelle (FR-16) sont AUTORITAIRES CÔTÉ SERVEUR. Hors-ligne, le client applique une résolution PROVISOIRE (depuis la carte mot-clé→rayon synchronisée) ré-arbitrée au flush. Un article ajouté hors-ligne peut donc changer de rayon au retour réseau — comportement attendu, pas un bug.
- (decision) PROPAGATION = Supabase Realtime (souscription par foyer), jamais de polling, jamais de rechargement manuel (FR-10/22/27). Le dashboard et les téléphones reflètent les changements en direct via Realtime ; hors-ligne, ils affichent le dernier état local + les actions en attente (pastille 'arrive…').
- (direction) DEFERRED noté : PowerSync = issue de secours si la réconciliation maison devient douloureuse à l'usage. Condition de réveil : bugs de convergence récurrents en conditions réelles, ou passage hors du foyer (multi-foyers, plus d'utilisateurs).
- (decision) CONTRAT COMMUN = Postgres est la source unique (ratifié). RLS + contraintes + fonctions SQL = vérité et application. PostgREST (lire par rayon via vue grocery_list_by_aisle, ajouter, cocher, supprimer, archiver, vider) + Realtime = l'API stable versionnée (FR-19-23). Chaque surface (web/dashboard/téléphone/MCP/pont) = adaptateur MINCE. Binds : où toute règle métier vit. Prevents : deux surfaces appliquant des règles divergentes ; toute règle en double.
- (decision) AUTORITÉ UNIQUE : Postgres est la seule autorité. Toute règle côté client (résolution de rayon provisoire hors-ligne, décision 1) est PROVISOIRE et jamais autoritaire — ré-arbitrée au flush. Interdit : un chemin d'écriture qui contourne la RLS (donc INTERDICTION du SUPABASE_SERVICE_KEY côté MCP/pont — dénoue la tension FR-39 structurellement, pas par vigilance).
- (decision) IDENTITÉ = jetons d'appareil scopés au foyer. Chaque surface non-humaine (dashboard, pont Google, client MCP, Shortcut éventuel) = une identité d'appareil : ligne device_credentials (id, household_id, kind, scope restreint, created_by, revoked_at), et un jeton portant le claim household_id. Binds : le modèle d'autorisation de toute surface sans user. Prevents : un appareil traité comme une personne (NFR-6), une révocation impossible à l'unité, un dashboard qui exige un login.
- (decision) RLS ÉTENDUE : current_household_id() résout le foyer depuis SOIT le profil humain (profiles.id = auth.uid()), SOIT le claim household_id du jeton d'appareil (auth.jwt()). Révocation INSTANTANÉE même jeton non expiré : les politiques joignent device_credentials et exigent revoked_at IS NULL. Binds : toutes les politiques RLS existantes (à faire évoluer sans casser l'isolation). Prevents : un jeton révoqué qui continue d'agir.
- (decision) APPAIRAGE DASHBOARD (question ouverte PRD n°1, déléguée par l'UX) : Florian, connecté sur le web, émet une identité d'appareil 'dashboard' → obtient une URL/jeton qu'il ouvre UNE fois sur l'écran mural → l'écran persiste le jeton → ne redemande JAMAIS de login (FR-28). Révocable depuis le web. Aucun mécanisme touché par la conjointe.
- (decision) AUTH HUMAINE = magic link SANS mot de passe (FR: invariant produit perçu par la conjointe, PAS un détail technique révocable par un архitecte). [ADOPTED] par décision d'origine MAIS le code a dérivé (email+mot de passe) → à réaligner en Lot 0. Binds : le seul mode d'authentification humaine. Prevents : réintroduction d'un mot de passe qui déplace le test d'acceptation 'elle ne configure rien'.
- (decision) PONT GOOGLE = job d'ingestion à identité d'appareil, UNIDIRECTIONNEL, IDEMPOTENT (marque-sans-supprimer côté Google, purge périodique). NutriClaude ne fait JAMAIS confiance à Google comme source de lecture (FR-50). Latence ~60s STRUCTURELLE (NFR-4) — aucune surface ne laisse croire qu'un article dicté est déjà arrivé. Rupture du pont → foyer prévenu côté web/Claude (FR-49), accumulation côté Google récupérée au rétablissement (FR-48). Secrets Google : compte dédié, chiffré, révocable, master token (NFR-12).
- (decision) SHORTCUTS iOS = surface de premier plan ADOPTÉE. Un Shortcut = une identité d'appareil (jeton scopé foyer) qui appelle l'API. Donne un chemin vocal 'Dis Siri, ajoute X' mains-libres sur iPhone, INDÉPENDANT de Google (repli FR-48/49, satisfait NFR-6 'raccourcis'). App Raccourcis préinstallée : aucun store, aucun binaire natif, conforme NFR-11. iOS-only assumé (équivalent Android via routines possible, non porté activement). Binds : existence d'une surface cliente HTTP directe supplémentaire. Prevents : dépendance vocale unique à un tiers (Google).
- (decision) CLIENT PWA (pivot acté) : les surfaces liste LISENT/ÉCRIVENT via le client Supabase du navigateur (RLS-enforced) + outbox local IndexedDB — PAS via Server Actions (réseau requis = incompatible offline). Next 16 = coquille PWA : service worker (precache app-shell, données en IndexedDB), manifeste, cible de partage web. Server Actions/route handlers réduites à l'irréductible serveur : callback magic-link, émission jetons d'appareil + invitations. Binds : le chemin de données de toute surface liste. Prevents : retour au tout-force-dynamic ; un chemin d'écriture qui exige le réseau.
- (constraint) Cible de partage web (FR-33/46) : fiable sur Android, LIMITÉE sur iOS PWA → le chemin iOS mains-libres passe par le Shortcut adopté (décision 5). À documenter comme asymétrie de plateforme assumée.
- (decision) OPS = Supabase (Postgres, Auth, Realtime, Edge Functions, pg_cron) + Vercel (coquille Next 16, Fluid Compute, previews). Le PONT tourne en Edge Function Supabase déclenchée par pg_cron (~60s). Un seul projet Supabase (prod) ; dev local via Supabase CLI. Binds : l'enveloppe de déploiement. Prevents : un serveur à demeure à maintenir (NFR-10), une infra de sync en plus (NFR-11).
- (decision) STACK ratifiée (cibles, à re-confirmer web avant gel) : Next 16.2, React 19.2, Tailwind 4.2, TypeScript 6, @supabase/ssr + supabase-js. Vercel + Supabase.
- (decision) DÉBLOCAGE LOT 0 (prérequis, l'app ne compile pas) : (1) TAILWIND 4 EN AVANT — installer @tailwindcss/postcss, migrer globals.css en @import + @theme (porte nativement les 2 thèmes clair/sombre de DESIGN.md), retirer tailwind.config.ts JS et le dark #0f1117 câblé ; (2) cookies() → await (Next 16) dans lib/supabase/server.ts ; (3) middleware.ts → convention proxy (Next 16) en préservant le gating d'accès ; (4) baseUrl retiré (TS 6). Binds : la capacité à builder. Prevents : toute story Lot 1 démarrée sur un build cassé.
- (constraint) DETTE code à réaligner (Lot 0/1, tracée par reconcile-code.md) : cocher/décocher cassé (toggle relatif codé en dur) → refonte en status posé idempotent ; FR-5 agrégation doublons inexistante ; FR-4 rayon auto non câblé à l'ajout manuel ; génération qui DELETE les 'pending' existants (doit devenir non destructrice, FR-17) ; meal_plan_entries sans unicité (doublons → quantités doublées) ; aucun error.tsx/not-found.tsx (viole NFR-8) ; auth email+mdp → magic link ; DELETE dur → tombstones.
- (event) Reviewer Gate exécutée. Lint déterministe : 0 finding. Versions : toutes confirmées courantes/cohérentes sur le web (Next 16.2 GA, React 19.2 pair officiel, Tailwind 4.2, TS 6 GA). Rubric : dimension muette FR-40-43 (foyer/invitation/profil humain) + FR-17 non-invariant + unicité meal_plan + ops/test. Adversarial : 2 critiques (LWW-par-champ non soutenue par le schéma ; agrégation qui détruit l'id de la LWW/tombstone) + 3 high/medium (génération vs tombstone, dédup pont, provenance polymorphe).
- (decision) COLMATAGES GATE appliqués. A: ligne canonique UNIQUE par (household_id, nom normalisé, unité), 'ajouter'=UPSERT-incrément (dissout la fusion-qui-détruit-l'id). B: LWW PAR CHAMP arbitrée par HORODATAGE D'INTENTION CLIENT (pas l'arrivée serveur), max par champ mutable. C: génération non destructive posée en invariant (FR-17), résurrection d'un tombstone seulement si intention plus récente. D: colonne source_ref pour dédup pont + normalisation unité d'ingestion. E: provenance FR-7 POLYMORPHE profil-ou-appareil (jamais promouvoir un appareil en personne). F: NOUVEL AD foyer & appartenance humaine (FR-40-43) + Deferred 'foyer à vie'. G: unicité meal_plan_entries. H: ops (backups Supabase, logs) + posture de test nommée (RLS, convergence). I: pont via pg_cron + pg_net(net.http_post)+Vault.
- (event) spine finalized
- (event) Design de solution HTML produit (solution-design.html) — livrable humain choisi par Florian, aux couleurs NutriClaude. Déroule paradigme, sécurité structurelle, convergence hors-ligne (exemple 09h00/09h05/09h30), voix double chemin, ops, déblocage Lot 0, deferred.
