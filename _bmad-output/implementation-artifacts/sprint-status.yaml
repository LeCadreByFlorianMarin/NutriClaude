# generated: 2026-07-25
# last_updated: 2026-07-25
# project: nutriclaude
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones

generated: 2026-07-25
last_updated: 2026-08-20  # 2026-08-20 — Story 4.5 REVUE puis FERMÉE (`done`). Revue adversariale
                          # à trois couches parallèles. ⚠️ MÊME MODÈLE QUE L'IMPLÉMENTATION — la
                          # règle §6 recommande un autre LLM ; les convergences entre couches
                          # indépendantes ont donc servi de signal, et elles ont porté.
                          # Bilan : 2 décisions tranchées par Florian, 14 correctifs appliqués,
                          # 5 reports, 3 constats écartés.
                          # ⛔ LE DÉFAUT LE PLUS GRAVE, trouvé par DEUX couches indépendamment et
                          # reproduit une 3e fois au triage : les trois fonctions SQL ne portaient
                          # AUCUN filtre de foyer. En `service_role` (qui contourne la RLS) avec le
                          # JWT du foyer A, `vider_la_liste()` rendait 11 et vidait le foyer B.
                          # Atteignabilité alors : NULLE (aucun service_role dans app/lib/proxy),
                          # mais le `comment on function` promettait « du foyer courant » et
                          # suppression.ts annonçait ces RPC pour le dashboard (E5) et MCP (E7) —
                          # les surfaces à clé de service. CORRIGÉ par une migration NEUVE
                          # (20260820090000), et PROUVÉ : rend 1, foyer B intact.
                          # ⚠️ L'en-tête de 20260817160000 affirmait qu'un filtre de foyer
                          # contredirait AD-1/AD-2. C'est daté plutôt qu'effacé : AD-1/AD-2
                          # interdisent qu'une SURFACE se porte garante, pas qu'un `where` vive
                          # dans une fonction SQL — qui EST la base.
                          # ✅ graphql_public retiré de config.toml : `supabase gen types --local`
                          # reproduit désormais EXACTEMENT le fichier du dépôt (0 ligne de diff).
                          # ⛔ TROIS ÉCARTS DE MÉTHODE, plus instructifs que les défauts :
                          # (1) la story revendiquait des règles qu'elle ne s'appliquait pas —
                          #     « une région de statut par zone d'action » (la ligne n'en avait
                          #     pas), « un compte rendu ment dès qu'un geste est armé » (il ne
                          #     mentait pas qu'à l'armement), « un état vide se mérite » (il
                          #     s'affichait pendant un vidage EN VOL) ;
                          # (2) les motifs étaient cités mais partiellement repris — ListeRayons
                          #     porte la restitution du focus AVEC sa raison écrite, elle n'avait
                          #     pas été reprise ; InviteCard ne porte pas de sr-only, il avait été
                          #     ajouté et doublait l'annonce ;
                          # (3) UNE CASE COCHÉE À TORT : D1 exigeait de DATER EXPERIENCE.md:104.
                          #     Le fichier n'avait pas été touché — la réécriture ne vivait que
                          #     dans un commentaire de CSS, ce que D1 nommait « contredire en
                          #     silence ». EXPERIENCE.md:104 et :142, DESIGN.md:279 et :298 sont
                          #     désormais datés.
                          # ⛔ ET LE SECOND PARCOURS À L'ŒIL A TROUVÉ 2 DÉFAUTS DANS MES PROPRES
                          # CORRECTIFS, tous deux verts aux six portes : la zone de statut de ligne
                          # posée en `basis-full` dans un flex sans wrap rendait LES LIBELLÉS UNE
                          # LETTRE PAR LIGNE ; et ce message n'apparaissait sur aucun chemin réussi
                          # (la ligne est retirée optimistement, sa zone part avec elle).
                          # Portes : npm test 271/271 · isolation 124, 123 pass, 0 fail, 1 skipped
                          # (+3) · typecheck · lint · check:migrations 20/18/2/0 · build 14 routes.
                          # ⚠️ « Un instant… » NON OBSERVÉ : l'écriture locale rend en quelques ms.
                          # Le chemin est corrigé, mais je ne l'ai pas vu — écrit plutôt que supposé.
                          # ⛔ TOUJOURS OUVERT : la décision `intent_at`, à trancher AVANT la 4.10.
                          #
                          # 2026-08-19 — Story 4.5 FUSIONNÉE ET DÉPLOYÉE (PR #31, squash `fefa2ec`). Les
                          # stories 4.1 à 4.5 sont dans `main` ; aucune PR ouverte, aucune branche
                          # de travail restante. Les 4 contrôles de la PR étaient verts.
                          # ⛔ LA REQUÊTE DE CONTRÔLE EN PRODUCTION N'A PAS ÉTÉ EXÉCUTÉE — décision
                          # de Florian, consignée en commentaire de la PR plutôt qu'effacée. Ce qui
                          # borne le risque EST mesuré, mais sur le CODE et non sur les données :
                          # aucun chemin applicatif n'écrit `deleted_at` (git grep sur app/ et lib/
                          # ne rend qu'un commentaire et les types générés), et la seule migration
                          # qui le pose non nul est celle de cette story. Un tombstone en production
                          # ne pourrait venir que d'un SQL manuel.
                          # ⚠️ REQUÊTE À LANCER SI UN DÉPLOIEMENT ÉCHOUE (`violeraient` doit être 0) :
                          #   select count(*) filter (where deleted_at > now() + interval '1 day')
                          #     as violeraient from grocery_list_items;
                          # ✅ RÉSOLU LE 2026-08-19 — ET LA MIGRATION EST EN PRODUCTION.
                          # L'anomalie : la fusion de la #31 (18:17:48Z) n'avait déclenché NI la CI
                          # NI le déploiement. Mesuré 3 h après : 0 run pour `fefa2ec`, 0 en file,
                          # workflow `active`, GitHub « All Systems Operational ».
                          # La fusion SUIVANTE (#32, `e0ad6c5`, 05:41:31Z) a tout déclenché
                          # normalement : CI verte sur `main`, déploiement de production `Ready`
                          # en 32 s. ⚠️ LA CAUSE DU PREMIER RATÉ RESTE INCONNUE — écrit plutôt
                          # qu'inventé. Ce qui est mesuré : l'événement `pull_request` fonctionnait
                          # (les contrôles de la #32 sont partis normalement), seul le push de
                          # fusion de la #31 n'a rien produit.
                          # ✅ ET C'EST CE DÉPLOIEMENT QUI RÉPOND À LA REQUÊTE DE CONTRÔLE NON
                          # EXÉCUTÉE. Journal de build, verbatim :
                          #   [migrations] déploiement de production — application des migrations
                          #   • 20260817160000_supprimer_archiver_vider_la_liste.sql
                          #   Applying migration 20260817160000_...sql...
                          #   [migrations] appliquées.
                          # Le volet 1 pose une contrainte VALIDÉE contre les lignes existantes :
                          # une seule ligne au `deleted_at` lointain aurait rendu 23514 et cassé le
                          # build. Il est passé — donc `violeraient` valait bien 0 en production.
                          # ⚠️ C'est une preuve PAR L'APPLICATION, obtenue après coup ; elle ne
                          # remplace pas le contrôle AVANT fusion que le gabarit demande.
                          #
                          # 2026-08-17 (4) — Story 4.5 IMPLÉMENTÉE (statut `review`). D1 tranchée
                          # par Florian (option a) ; D2 à D5 sur leur défaut prescrit.
                          # ⛔ LE FAIT CENTRAL EST DÉSORMAIS FIGÉ PAR UN TEST : `.delete()` rend
                          # `DELETE 0` SANS ERREUR sur grocery_list_items. Toute surface future
                          # (dashboard E5, MCP E7, pont E6) doit passer par le tombstone.
                          # ⛔ LE DÉFAUT « 14 POMMES » EST FERMÉ À LA SOURCE : la quantité d'une
                          # ligne tombstonée ne s'additionne plus au réajout — et SEULEMENT là,
                          # un acheté encore vivant continue de s'additionner (borné par un test).
                          # ⚠️ CONSÉQUENCE POUR LA 4.7 : elle ressuscite des lignes tombstonées,
                          # donc elle hérite de cette règle.
                          # ⛔ UN TEST PRÉEXISTANT DE LA 4.4 A CHANGÉ D'ASSERTION (3 → 2), mis à
                          # jour avec sa raison datée plutôt qu'effacé.
                          # ⛔ UN REPORT EST RÉSOLU PAR UNE DÉCISION, PAS UNE CONTRAINTE :
                          # `(status, deleted_at)` reste non contraint — `bought` + tombstone EST
                          # l'archivage, et le contraindre casserait l'AC2.
                          # ⛔ TROIS DÉFAUTS INTRODUITS PUIS CORRIGÉS, consignés : `String.replace`
                          # a mangé les `$$` de la fonction SQL (voisin du piège `\uXXXX` de la
                          # 4.4) · deux tests d'isolation supposaient être seuls au monde — LE
                          # PRÉFIXE UNIQUE ISOLE LES NOMS, PAS LES COMPTEURS DE FOYER · la première
                          # sonde CSS lisait un fichier VIDE et rendait 0 par absence de données.
                          # ⛔ LE PARCOURS À L'ŒIL A TROUVÉ DEUX DÉFAUTS QUE 271 TESTS ET SIX
                          # PORTES NE VOYAIENT PAS : le compte rendu du vidage était démonté dans
                          # le rendu qui l'écrivait, et L'ÉTAT VIDE ÉTAIT UN CUL-DE-SAC (plus de
                          # formulaire d'ajout après avoir vidé sa liste). Le second est ANTÉRIEUR
                          # à cette story — dormant jusqu'à ce que « Tout enlever » le rende
                          # atteignable — et sa correction referme l'écart que la 4.2 avait daté
                          # à l'intention de la 4.4 (« lien vers l'ajout sur l'état vide »).
                          # Portes : npm test 271/271 (+5) · isolation 121, 120 pass, 0 fail,
                          # 1 skipped (+10) · typecheck · lint · check:migrations 19/17/2/0 ·
                          # build 14 routes · sonde CSS 3 classes, 5 contrôles négatifs à 0 et
                          # 4 TÉMOINS POSITIFS · banc de mutations 3/3 tués.
                          # ⚠️ types.ts régénéré : le diff N'EST PAS purement additif — il retire
                          # `graphql_public`, que deferred-work.md signalait comme entré dans le
                          # contrat PostgREST sans qu'aucune story l'ait décidé. Aucun fichier
                          # applicatif ne le référence (mesuré).
                          # ✅ PARCOURS À L'ÉCRAN COMPLET, aux DEUX thèmes, thème remis, après que
                          # Florian a coupé le serveur du port 3333. Les trois correctifs sont
                          # REVÉRIFIÉS à l'œil, dont D3 : « Pommes » vidé puis réajouté à 4 rend
                          # « 4 pièces », pas 10. NFR-3 : 0 débordement à 390/360/320 px, PIRE CAS
                          # compris (confirmation armée + nom de 200 caractères insécables).
                          # Contraste de la classe neuve `.btn-ligne` : 5,713:1 clair · 8,059:1
                          # sombre · cible tactile 44 px · anneau de focus global non annulé.
                          # ⚠️ MA PREMIÈRE MESURE SOMBRE ÉTAIT FAUSSE (2,03:1) : elle ignorait
                          # l'alpha du fond de carte. Recomposée couche par couche, elle retombe
                          # sur rgb(38,29,35) = #261d23, le « verre » que DESIGN.md nomme.
                          # ⚠️ NOTE D'OUTILLAGE QUI RESSERVIRA, et qui CORRIGE le diagnostic de la
                          # 4.4 : les cookies ne distinguent pas les PORTS. Un second serveur du
                          # projet connecté à la PRODUCTION partage sa session avec le serveur de
                          # parcours, et une session de production laisse le rendu serveur suspendu
                          # sur requireProfile() — page bloquée sur son loading.tsx, React jamais
                          # hydraté, PAS UNE SEULE ERREUR EN CONSOLE. La 4.4 avait conclu « c'était
                          # le serveur de développement » : passer en production avait marché parce
                          # que ça changeait le PORT, donc la collision. ⚠️ 127.0.0.1 ne suffit pas,
                          # la redirection d'authentification ramène sur localhost.
                          # ⚠️ AUCUNE ÉCRITURE N'A ATTEINT LA PRODUCTION : bundle client construit
                          # sur l'URL locale (vérifié dans les fragments), et chaque geste
                          # recontrôlé en psql sur la base locale.
                          # ⛔ TOUJOURS OUVERT, INCHANGÉ : la décision `intent_at` — D2(a) l'a
                          # ÉVITÉE pour cette story, elle ne l'a pas refermée. À trancher AVANT
                          # la 4.10.
                          #
                          # 2026-08-17 (3) — Story 4.5 CONTEXTUALISÉE (`backlog` → `ready-for-dev`),
                          # sur `452f308`. Dix mesures exécutées sur le stack local, en
                          # `begin … rollback`.
                          # ⛔ LE FAIT CENTRAL : `.delete()` NE RATE PAS, IL MENT. Le privilège
                          # de table DELETE est accordé à `authenticated` (M2) mais AUCUNE
                          # politique RLS ne l'autorise (M1) — un DELETE rend donc `DELETE 0`
                          # SANS ERREUR et la ligne survit (M3). PostgREST rendrait `error: null` :
                          # un écran optimiste montrerait l'article disparu, et il reviendrait au
                          # chargement suivant sans qu'aucune porte ne le dise. Supprimer est un
                          # `UPDATE deleted_at`, jamais un DELETE.
                          # ⛔ UN SECOND DÉFAUT, MESURÉ, QUE CETTE STORY CRÉE SI ELLE NE LE FERME
                          # PAS : 10 articles achetés → archivés → réajoutés à 4 donnent **14**
                          # (M7). `ajouter_article` additionne sur la quantité d'une vie
                          # précédente, l'index canonique étant TOTAL. La 4.5 est la première
                          # story à écrire des tombstones, donc la première à rendre ce chemin
                          # atteignable — d'où D3.
                          # ⚠️ CINQ DÉCISIONS OUVERTES, chacune avec son défaut prescrit. D1 est
                          # la plus structurante et elle est POSÉE NOMMÉMENT dans le code :
                          # `ListeCourses.tsx:480-482` dit « la suppression est la story 4.5, et
                          # elle devra trancher ce point plutôt que le contourner » —
                          # `EXPERIENCE.md:104` impose un seul hit-target par ligne, et la
                          # maquette ne tranche pas (ses deux seuls `×` sont des quantités).
                          # D2 : fonctions SQL `security invoker`, pour ne PAS élargir le défaut
                          # `intent_at` à un troisième chemin d'écriture — et sur `deleted_at`,
                          # c'est-à-dire le champ même que la 4.10 arbitre.
                          # D4 : borne haute de `deleted_at` (M5 : `now() + 100 ans` accepté),
                          # report daté de la 4.1 et adressé nommément à cette story.
                          # ⛔ UN REPORT EST RÉSOLU PAR UNE DÉCISION, PAS PAR UNE CONTRAINTE :
                          # `(status, deleted_at)` reste délibérément non contraint, parce que
                          # `bought` + tombstone EST l'archivage (M6) — le contraindre casserait
                          # l'AC2. Écrit dans la migration pour qu'une revue ne le « répare » pas.
                          # ⚠️ D5 : « Vider le panier » (source verbatim) et « Vider la liste »
                          # sont trop voisins pour deux portées différentes ; la seconde chaîne
                          # est INVENTÉE et signalée comme telle, comme la 4.3 l'avait fait.
                          # Portes au départ : npm test 266/266 · isolation 111, 110 pass, 0 fail,
                          # 1 skipped · check:migrations 18/16/2/0.
                          # ⛔ TOUJOURS OUVERT, INCHANGÉ : la décision `intent_at` (horloge client
                          # vs serveur, +0,740 s mesuré) — à trancher AVANT la 4.10. D2 l'évite
                          # pour cette story, elle ne la referme pas.
                          #
                          # 2026-08-17 (2) — Story 4.4 FUSIONNÉE (PR #29). Les stories 4.1 à 4.4
                          # sont `done` et dans `main` ; aucune PR ouverte, aucune branche de
                          # travail restante.
                          # ⛔ UNE DÉCISION RESTE OUVERTE, ET ELLE EST DANS `main` : `intent_at`
                          # mélange l'horloge du CLIENT (bascule de la 4.3) et celle du SERVEUR
                          # (insertion, et fonction SQL de la 4.4). Écart mesuré : +0,740 s entre
                          # le conteneur Postgres et l'hôte, donc une coche peut écrire un
                          # horodatage ANTÉRIEUR à l'insertion et l'arbitre du LWW recule.
                          # Invisible en CI (pas de décalage sur un runner), réel entre deux
                          # téléphones. Elle touche AD-3, la 4.9 (horloge du geste, outbox) et la
                          # 4.10 (arbitrage). À trancher AVANT la 4.10, qui en dépend entièrement.
                          # ⚠️ Deux autres reports à traiter en chemin : R2-6 (contraste des
                          # cartes en sombre — jugé lisible à l'œil, mesure toujours sous 3:1, à
                          # trancher dans DESIGN.md avant la 4.13) et l'arrondi des quantités
                          # mises à l'échelle (FR-52 → FR-16, donc story 4.7).
                          #
                          # 2026-08-17 — PARCOURS À L'ÉCRAN DES TROIS STORIES, enfin fait, et
                          # les 4.2 / 4.3 passent à `done` (PR #27 et #28 fusionnées).
                          # ⛔ IL A FALLU UNE CONSTRUCTION DE PRODUCTION : quatre tentatives sur
                          # le serveur de DÉVELOPPEMENT échouaient toutes de la même façon — page
                          # bloquée sur son squelette, JS chargé mais effet jamais exécuté.
                          # `next build` + `next start` a réglé le problème d'un coup. C'ÉTAIT
                          # L'OUTILLAGE, PAS L'APPLICATION — la troisième famille de la règle §7,
                          # « l'outillage de test lui-même », et elle a coûté quatre diagnostics.
                          # ✅ VU DANS LES DEUX THÈMES : ombre du squelette · accord « 10 pièces » ·
                          # séparateur présent quand il sépare et absent quand tout est acheté ·
                          # barré lisible · hit-target à 389 px de la case, ligne de 46 px ·
                          # compteur 6→5 et ratio 1/3→2/3 · LE CORRECTIF DU PANIER DE LA 4.3
                          # VÉRIFIÉ (ordre de la base) · L'AGRÉGATION DE LA 4.4 VÉRIFIÉE
                          # (« Pommes 6 pièces » + 4 → « 10 pièces », UNE ligne) · refus de
                          # quantité nommant sa cause, bouton qui ne saute pas (`reserve`) ·
                          # bouton abricot 44 px · 0 débordement à 390/360/320 px.
                          # ⚠️ RÉSERVE : la fenêtre a refusé de se redimensionner, NFR-3 est
                          # mesuré en contraignant le conteneur — un proxy, pas un viewport.
                          # ✅ R2-6 TRANCHÉ À L'ŒIL : la pile sombre se lit, portée par le
                          # remplissage de carte plus clair que le fond, pas par la bordure. La
                          # mesure reste vraie (rien n'atteint 3:1) — à trancher dans DESIGN.md
                          # avant la 4.13, non bloquant ici.
                          # ⛔ TOUJOURS OUVERT : la décision `intent_at` (horloge client vs
                          # serveur, +0,740 s mesuré) — elle touche AD-3, la 4.9 et la 4.10.
                          #
                          # 2026-08-16 (2) — Story 4.4 IMPLÉMENTÉE (statut `review`), sur une
                          # branche `feat/4-4`. Quatre décisions prises sur leur défaut prescrit.
                          # ✅ L'agrégation est MESURÉE de bout en bout : deux ajouts du même
                          # article → 1 ligne quantité 3 · casse/accents/espaces plient · unité
                          # différente → 2 lignes · réajout sur un TOMBSTONE ACHETÉ → 1 ligne,
                          # `pending`, tombstone levé (le cas qui rendait 23505 à un INSERT).
                          # ⛔ DEUX DÉFAUTS INTRODUITS PUIS CORRIGÉS, consignés :
                          #  1. la signature de `resolve_aisle_id` était SUPPOSÉE (4 arguments,
                          #     pas 2) — et la requête de contrôle n°2 de l'en-tête existait pour
                          #     ça, non exécutée avant d'écrire ;
                          #  2. la contrainte `deleted_at >= created_at` a CASSÉ un test
                          #     préexistant, et la cause est LE MÊME DÉFAUT D'HORLOGE QUE LA 4.3 :
                          #     created_at serveur, deleted_at client, +0,740 s d'écart. Une borne
                          #     stricte aurait empêché tout membre au téléphone qui retarde de
                          #     supprimer. Corrigée avec la tolérance d'un jour, reprise de
                          #     grocery_list_items_intention_bornee.
                          # ⚠️ D1(a) referme le défaut `intent_at` hérité de la 4.3 pour les
                          # écritures de CETTE story : now() en SQL est l'horloge serveur.
                          # ⚠️ L'arrondi de l'AC3 n'est PAS implémenté : FR-52 le rattache à
                          # FR-16, donc à la story 4.7. Écrit plutôt qu'esquivé.
                          # Portes : npm test 266/266 (+8) · isolation 111, 110 pass, 0 fail,
                          # 1 skipped (+8) · typecheck · lint · check:migrations 18/16/2/0 ·
                          # build 14 routes · sonde CSS 4 classes neuves, 3 contrôles négatifs à 0.
                          # ⛔ RESTE DÛ : le parcours à l'écran — celui des stories 4.2 et 4.3 est
                          # dû aussi ; les trois se font d'un seul passage.
                          #
                          # 2026-08-16 — Story 4.4 CONTEXTUALISÉE (`ready-for-dev`), sur
                          # `4e65beb`. Douze mesures exécutées en `begin … rollback`.
                          # ⛔ LE FAIT CENTRAL, mesuré trois fois : l'index unique canonique est
                          # TOTAL — un article ACHETÉ comme un TOMBSTONE occupent la clé et font
                          # rendre 23505 à un INSERT, alors que la vue ne les montre pas.
                          # RÈGLE POUR TOUTES LES SURFACES : lire la TABLE, jamais la vue, avant
                          # tout ajout. Et « ajouter » est un UPDATE dès que la clé est occupée,
                          # quel que soit le status (AD-6 : jamais un INSERT nu).
                          # ✅ AD-6 tranche le fond (agrégation autoritaire côté serveur), et une
                          # contrainte technique élimine la voie client : PostgREST ne sait pas
                          # INCRÉMENTER en upsert, son on_conflict écrase. D'où D1 : une fonction
                          # SQL `security invoker` — jamais `definer`, c'est le trou de
                          # seed_default_aisles.
                          # ⚠️ DEUX FENÊTRES BON MARCHÉ SE REFERMENT ICI, la table étant encore
                          # vide en production : `quantity >= 0` (mesuré : -5 est accepté
                          # aujourd'hui) et `deleted_at >= created_at` (adressée à la 4.5).
                          # ⚠️ L'arrondi de l'AC3 n'appartient probablement pas à cette story :
                          # FR-52 le rattache à FR-16, la génération — donc la 4.7.
                          # ⛔ DÉFAUT OUVERT HÉRITÉ, et il la concerne : `intent_at` mélange
                          # horloge client (4.3) et horloge serveur (insertion) — écart mesuré de
                          # +0,740 s entre le conteneur Postgres et l'hôte. Une fonction SQL le
                          # résout gratuitement : argument de plus pour D1(a).
                          #
                          # 2026-08-13 — Story 4.3 IMPLÉMENTÉE (statut `review`). Les QUATRE
                          # décisions prises sur leur défaut prescrit, faute d'arbitrage : vue
                          # élargie · vraie coche ici · `intent_at` écrit · optimiste + rollback.
                          # ⛔ UNE MIGRATION, et c'est elle qui rend la story possible : la vue
                          # filtrait `status = 'pending'`, donc cocher faisait DISPARAÎTRE
                          # l'article et l'AC1 comme l'AC3 étaient inatteignables. Changement
                          # additif, `order by` inchangé (le tri du panier est un choix
                          # d'affichage, pas de contrat — l'Epic 5 et l'Epic 7 liront cette vue).
                          # ⚠️ `status` a dû être ajouté à la chaîne `.select()` : élargir la vue
                          # ne suffisait pas, l'oubli aurait rendu `statut` undefined EN SILENCE.
                          # ⚠️ `types.ts` NON régénéré, mesuré : le bloc de type de la vue est
                          # identique (des lignes de plus, pas des colonnes).
                          # ⛔ UN ÉTAT NEUF EST NÉ SANS COPIE ÉCRITE — « tout est pris » : des
                          # articles, aucun en pending. Réutiliser « Ta liste est vide. » y aurait
                          # été faux. Rédigé « Tout est dans le panier. », chaîne INVENTÉE par le
                          # dev agent et non reprise d'EXPERIENCE.md — à valider en revue.
                          # ✅ La note laissée par la 4.2 à la story 4.5 tombe d'elle-même : le
                          # dénominateur du ratio ne décroît plus (1/4 → 2/4, pas 0/4 → 0/3).
                          # Portes : npm test 257/257 (+8) · isolation 103, 102 pass, 0 fail,
                          # 1 skipped (+5) · typecheck · lint · check:migrations 17/15/2/0 ·
                          # build 14 routes · sonde CSS 4 classes neuves, 6 contrôles négatifs à 0
                          # · contraste de la coche MESURÉ (3,638:1 clair, 4,359-4,768:1 sombre,
                          # seuil 3:1 tenu partout).
                          # ⛔ RESTE DÛ, et aucune porte ne le voit (§7) : LE PARCOURS À L'ÉCRAN,
                          # laissé non coché avec sa raison. Celui de la 4.2 l'est aussi — les
                          # deux peuvent se faire d'un seul passage.
                          #
                          # 2026-08-12 (2) — Story 4.3 CONTEXTUALISÉE (`ready-for-dev`), sur
                          # `5b43c77`. Seize mesures exécutées sur le stack local, dont une qui
                          # change la FORME de la story : ⛔ M3 — cocher un article le fait
                          # DISPARAÎTRE de la vue (`where status = 'pending'`), donc l'AC1
                          # (« dans les deux sens ») et l'AC3 (« reste consultable ») sont
                          # inatteignables sans élargir la lecture. On ne décoche pas ce qu'on ne
                          # voit plus. ⚠️ Et le « panier » n'est PAS un écran : DESIGN.md:283 et
                          # EXPERIENCE.md:107 en font un SÉPARATEUR à l'intérieur de la carte-rayon.
                          # Quatre décisions ouvertes avec leur défaut prescrit : D1 visibilité des
                          # cochés (migration additive sur la vue) · D2 qui possède la coche, 4.3
                          # ou 4.13 (même forme que la D1 de la 4.2 : 2.4 construisait, 4.2
                          # consommait) · D3 `intent_at` écrit dès maintenant — M4bis mesuré : un
                          # UPDATE de `status` laisse l'arbitre du LWW PÉRIMÉ, et la 4.10 en dépend
                          # · D4 mise à jour optimiste avec rollback.
                          # ✅ Rien à migrer pour la colonne ni la RLS : `status` (initial_schema)
                          # et `grocery_update` existent, les tokens de coche vide sont posés.
                          # ⚠️ État NEUF sans copie écrite : « tout est pris » — des articles, aucun
                          # en pending. Réutiliser « Ta liste est vide. » y serait faux.
                          #
                          # 2026-08-12 — Story 4.2 : SECONDE PASSE DE REVUE, portant sur la passe
                          # de correction du 2026-08-07 (règle §6 — « et la passe de correction
                          # doit être revue à son tour »). Statut ramené à `in-progress`.
                          # 27 constats retenus : 3 décisions, 20 correctifs APPLIQUÉS, 7 reports.
                          # ✅ Le CODE tient — les 3 AC, les 13 correctifs de la 1re passe, et rien
                          # à redire sur le regroupement, les nullités PostgREST, les tokens ni
                          # l'isolation. ⛔ Ce qui ne tenait pas était la REDDITION DE COMPTES :
                          # trois familles de chiffres périmés (dont ceux consignés ICI), une File
                          # List amputée de six fichiers, un token `--text-counter` déclaré livré
                          # et inexistant, une décision marquée « correctif » sans correctif, et
                          # une route d'échafaudage `app/zz-controle/` expédiée dans le build —
                          # elle se construisait en `○` et servait un faux « Ma liste » à tout
                          # membre connecté. Aucune porte automatique ne voyait l'un de ces six
                          # points : règle §7, cinquième fois sur ce projet.
                          # ⛔ D-4 MESURÉE (elle ne l'avait jamais été) : en thème sombre,
                          # bordure/carte 1,352–1,360:1, carte/gouttière 1,138:1 — AUCUNE grandeur
                          # n'atteint les 3:1 de WCAG 1.4.11, et les chiffres du report d'origine
                          # étaient faux dans le sens sévère. La pile serrée a changé le coupable :
                          # le séparateur réel de deux cartes n'est pas la bordure, c'est la
                          # gouttière de 14 px. Correctif reporté (R2-6) — il touche DESIGN.md et
                          # toute carte du produit, donc à trancher avant la story 4.3.
                          # PORTES REJOUÉES : npm test 247/247 · isolation 98 (97 pass, 0 fail,
                          # 1 skipped) · typecheck · lint · check:migrations 16/14/2/0 · build
                          # 14 routes · sonde CSS 21/21 avec 5 contrôles négatifs à 0 · banc de
                          # mutations 5/5 tuées, dont celle qui SURVIVAIT avant cette passe.
                          # ⚠️ RESTE DÛ AVANT FUSION, et aucune porte ne le voit : le PARCOURS À
                          # L'ÉCRAN, à refaire — la seconde passe a modifié le rendu en trois
                          # endroits visibles (ombre du squelette en thème clair, 24 px retirés
                          # au-dessus du compteur, « 2 pièces » au lieu de « 2 pièce »). Et avec
                          # lui le jugement d'œil que le calcul ne rend pas : la séparation des
                          # cartes se voit-elle en sombre sur la pile serrée ?
                          #
                          # 2026-08-07 — Story 4.2 IMPLÉMENTÉE (statut `review`). **Le premier
                          # écran du produit qui lit depuis le NAVIGATEUR** (AC1/AD-13) : les 20
                          # appels du client navigateur étaient jusqu'ici tous des écritures.
                          # D3/D4/D5 tranchées par Florian sur leurs défauts prescrits — lecture
                          # tout-client, « Ma liste » sur `/courses`, gros compteur livré ici.
                          # ⚠️ Le regroupement se fait par CLÉ, jamais par lignes consécutives :
                          # deux rayons ex æquo intercalent leurs articles, et le banc des
                          # mutations tue les 5 mutants dont celui-là. ✅ Parcours à l'œil fait
                          # aux deux réglages système, thème remis. NFR-3 mesuré à 390/360/320px,
                          # nom de 200 caractères insécable compris : aucun débordement.
                          # ⛔ DÉFAUT MAJEUR TROUVÉ EN CHEMIN, HORS PÉRIMÈTRE ET CONSIGNÉ :
                          # `generate_grocery_list_from_menu` fait SEGFAUTER PostgreSQL (un crash
                          # par appel, mesuré sur le stack local), et le test censé la garder passe
                          # pour la MAUVAISE RAISON — il observait l'erreur du crash, pas un refus
                          # de permission. La suite le cachait : ce test était le dernier du
                          # fichier, donc le crash tombait après sa dernière assertion.
                          # → `deferred-work.md`, adressé à la story 4.7.
                          # Portes : npm test 227/227 (+7), isolation 98/98 (+3), typecheck, lint,
                          # migrations 16/14/2/0 INCHANGÉ (aucune migration), build.
                          #
                          # 2026-08-07 (2ᵉ passe) — Story 2.4 REVUE UNE SECONDE FOIS, sur sa propre
                          # passe de correction (règle §6). Reste `done` : 3 décisions tranchées,
                          # 15 correctifs APPLIQUÉS, 7 reportés, 6 écartés. ✅ La 1ʳᵉ passe tient —
                          # 16 des 19 correctifs réellement fermés, six portes reproduites au
                          # chiffre près, banc des mutations 9/9 conforme. ⛔ Les 3 constats
                          # survivants étaient tous de la même forme : corrigés dans le CODE,
                          # oubliés dans le REGISTRE. Et la correction phare de la 1ʳᵉ passe
                          # surestimait sa portée — « la divergence est devenue inexprimable » est
                          # FAUX, mesuré : elle se réécrit en une ligne sous 216 tests verts.
                          # ⛔ Deux défauts de code NEUFS, encore dans le JSX qu'aucune porte
                          # n'exécute : `Boolean([])` vaut vrai — donc un rayon vide, cas nominal
                          # de l'AC2 et idiome exact de la 4.2, rendait une marge sous rien — et
                          # une jointure orpheline peignait la pastille abricot vide.
                          # ✅ D2 : `nomDeRayon`/`iconeDeRayon` sont désormais des ENVELOPPES de
                          # `lib/rayons/saisie.ts`, dont elles réimplémentaient la moitié.
                          # ⚠️ Défaut de RACINE reporté et mesuré : U+FE0F n'est pas exclu
                          # d'`INVISIBLES_HORS_JOINTURE`, donc ❤️ enregistre ❤ et 🏳️‍🌈 est démembré
                          # — À LA SAISIE (`lib/texte.ts`), pas à l'affichage. Story à créer.
                          # ⛔ Leçon de méthode : deux constats REJETÉS pour contamination croisée
                          # — une couche de revue avait créé une route de sonde et lancé un build,
                          # fabriquant la preuve qu'elle croyait lire. Portes : `npm test` 220/220
                          # (+4), isolation 95/95, typecheck, lint, migrations 16/14/2/0, build.
                          #
                          # 2026-08-07 — Story 2.4 REVUE puis FERMÉE (`done`). La story 4.2 est
                          # DÉBLOQUÉE POUR DE BON. ⛔ Les deux défauts majeurs étaient dans le JSX
                          # qu'aucune porte n'exécute : un `aria-label` sur un `<span>` nu
                          # (name-prohibited en ARIA 1.2, remède déjà écrit dans le dépôt), et un
                          # « banc des dents » qui déclarait tenu un invariant qu'aucun test ne
                          # peut tenir. ⚠️ Trois commentaires affirmaient un état FAUX de la base
                          # de code — quatrième epic d'affilée (règle §2). ✅ L'AC3 est désormais
                          # tenu EN ENTIER : route jetable, sept cartes, deux réglages système.
                          # ⚠️ Reste ouvert et daté : le composant en SITUATION, story 4.2.
                          # Détail à son entrée.
                          #
                          # 2026-08-06 — Story 2.4 IMPLÉMENTÉE (statut `review`), ce qui DÉBLOQUE
                          # la story 4.2. ⚠️ Son AC3 n'est tenu qu'à moitié — le rendu n'est
                          # éprouvé par rien, faute de consommateur. Détail à son entrée.
                          #
                          # 2026-08-06 — Story 2.4 contextualisée HORS ORDRE (`backlog` →
                          # `ready-for-dev`) : elle porte le composant carte-rayon que la décision
                          # D1 lui confie, et la 4.2 est arrêtée en attendant. Détail à son entrée.
                          #
                          # 2026-08-06 — Story 4.2 : D1 et D2 tranchées. ⛔ D1 → la story 2.4
                          # construit le composant carte-rayon, donc LA 4.2 EST BLOQUÉE tant que
                          # la 2.4 n'est pas `done`. ✅ D2 → les articles achetés partent à la 4.5.
                          #
                          # 2026-08-05 — Story 4.2 contextualisée : `backlog` → `ready-for-dev`.
                          # PREMIÈRE surface de l'Epic 4, et première lecture client-direct du
                          # dépôt. ⛔ CINQ décisions ouvertes, dont le composant carte-rayon que
                          # son critère dit de réutiliser et qui N'EXISTE PAS (story 2.4, backlog).
                          # Détail à son entrée.
                          #
                          # 2026-08-05 — Story 4.1 REVUE puis fermée (`done`), avec deux conditions
                          # ouvertes DATÉES : la requête de contrôle en production, et la fonction
                          # de génération toujours cassée (4.7). La revue a trouvé DEUX défauts
                          # bloquants — l'AC4 défait par une RPC, et l'AC2 faux pour 241 points de
                          # code invisibles. Détail à son entrée.
                          #
                          # 2026-08-05 — Story 4.1 IMPLÉMENTÉE (statut `review`). Détail à son entrée.
                          # Contexte d'origine : Story 4.1 contextualisée : `backlog` → `ready-for-dev`, et
                          # `epic-4` : `backlog` → `in-progress`. PREMIÈRE story de l'Epic 4, et la
                          # plus structurante du produit : elle pose la clé canonique sur laquelle
                          # 4.3, 4.4, 4.5, 4.6, 4.7, 4.10 et 4.12 s'arbitrent toutes. Aucune surface,
                          # aucune ligne de JSX — donc rien qui excuse un test manquant.
                          # ✅ Les 4 décisions TRANCHÉES avant démarrage, la première contre la
                          # recommandation : la clé canonique plie aussi les accents. Détail à
                          # son entrée, plus bas.
                          #
                          # 2026-08-05 — Story 7.1 contextualisée : `backlog` → `ready-for-dev`, et
                          # `epic-7` : `backlog` → `in-progress`. ⛔ QUATRE DÉCISIONS OUVERTES, dont
                          # le mécanisme d'identité lui-même. Ses trois critères citent les stories
                          # 5.1 et 5.7, qui n'existent pas. Détail à son entrée, plus bas.
                          # ⚠️ L'Epic 7 est pris HORS ORDRE : 4, 5 et 6 restent entiers en `backlog`.
                          #
                          # 2026-08-04 — Story 3.6 IMPLÉMENTÉE (statut `review`). Détail à son entrée, plus bas.
                          # Contexte d'origine : Story 3.6 contextualisée : `backlog` → `ready-for-dev`.
                          # DERNIÈRE story de l'Epic 3, et la PREMIÈRE qui ÉCRIT dans
                          # `meal_plan_entries`. Elle porte UNE migration — la contrainte d'unicité
                          # d'AD-6 que son AC2 nomme, ET la fermeture du trou de provenance de
                          # `recipe_id` que la 3.5 a mesuré et daté à son intention. Les deux dans
                          # le même fichier : `db push` n'est pas atomique sur un lot.
                          # ⚠️ TROIS PRÉMISSES ROUVERTES EN LES CITANT (règle §5), pas supposées
                          # closes : la moitié d'AC4 de la 3.5, le trou d'intégrité, et —
                          # ⛔ L'AC2 DE LA 3.5 EST EN PRODUCTION SANS AVOIR JAMAIS ÉTÉ OBSERVÉ.
                          # Mesuré : PR #22 fusionnée le 2026-08-04 à 13:34:53 UTC alors que le
                          # fichier de story disait « NE PAS FUSIONNER EN L'ÉTAT » et que sa Task 5
                          # était entièrement décochée ; corps de PR = gabarit vierge. Le parcours
                          # à l'écran de la 3.6 porte donc les DEUX — et cette story ajoute à cette
                          # grille un `<select>`, c'est-à-dire l'élément qui peut la faire déborder.
                          # ⚠️ UN TEST D'ISOLATION ASSURE AUJOURD'HUI QUE LE TROU EXISTE
                          # (`isolation.test.ts:1074`). La migration le fait tomber : son inversion
                          # est une tâche, pas un rangement — son propre commentaire l'avait prévu.
                          # ⚠️ Le texte de suppression d'une recette devient FAUX (cascade sur
                          # `meal_plan_entries`) : réparé dans le même commit, comme l'exige
                          # `deferred-work.md` qui l'adresse nommément à cette story.
                          # ✅ LES 4 QUESTIONS TRANCHÉES PAR FLORIAN LE 2026-08-04, AVANT DÉMARRAGE.
                          # (1) Politique `meal_plan_all` resserrée par un `with check` sur la
                          #     provenance — la clé étrangère composite est ÉCARTÉE (elle rendrait
                          #     l'embarquement PostgREST ambigu et casserait la grille livrée), et
                          #     le piège est DATÉ dans la story plutôt qu'effacé : c'est la forme
                          #     qu'une revue reproposera comme « plus propre ».
                          # (2) Route dédiée `/menu/[jour]/[repas]`, slug français porté par REPAS.
                          #     Ça supprime le risque AC2 À LA SOURCE — aucun `<select>` n'entre
                          #     dans une piste à 1/7 de largeur — et ça donne à la case vide la
                          #     « destination » que la 3.5 disait qu'elle attendait.
                          # (3) `meal_plan_entries_servings_positif` dans la même migration.
                          # ⚠️ (4) LE NOMBRE DE PERSONNES SE RÈGLE AU FOYER, PUIS S'AJUSTE PAR
                          #     ASSIGNATION — réponse HORS des options proposées, et elle élargit
                          #     le périmètre : + `households.default_servings`, + une section sur
                          #     `/foyer`, et c'est la PREMIÈRE écriture du produit dans `households`.
                          #     Conséquence à ne pas rater : la forme du schéma change, donc
                          #     `supabase gen types` est DÛ (trois contraintes seules n'auraient
                          #     rien changé). Et TROIS nombres de personnes coexistent désormais à
                          #     l'écran, dont deux s'appellent `servings` en base — c'est leur
                          #     RAPPORT que l'Epic 4 emploie, et les confondre rend le produit
                          #     incompréhensible au premier ajustement.
                          # ⚠️ Frontières posées avec la décision 4 : changer le réglage ne réécrit
                          #     AUCUNE assignation existante (pas d'`update` en masse) ; ce n'est ni
                          #     un réglage par membre (AD-16, foyer symétrique) ni par recette.
                          # ⚠️ Aucun test d'isolation neuf sur `households` : `:214-223` couvre déjà,
                          #     une politique RLS étant par ligne et pas par colonne.
                          # Périmètre passé de 9 à 10 tâches. TOUTES OUVERTES, plus rien n'attend.
last_updated: 2026-08-04  # 2026-08-04 — Story 3.5 contextualisée, tranchée, puis IMPLÉMENTÉE (statut `review`).
                          # ⛔ Le parcours à l'écran n'a pas eu lieu : l'AC2 n'est pas démontré.
                          # Détail à l'entrée de la story 3.5, plus bas. Contexte d'origine :
                          # Story 3.5 contextualisée : `backlog` → `ready-for-dev`,
                          # puis ses TROIS QUESTIONS TRANCHÉES PAR FLORIAN avant démarrage.
                          # Les sept tâches sont ouvertes, plus rien ne bloque.
                          # ⚠️ LA 3.4 EST SAUTÉE À LA DEMANDE DE FLORIAN et reste `backlog` —
                          # conséquence directe : les étiquettes de la maquette `grille-menu.html`
                          # n'existent pas et ne sont pas de cette story.
                          # ⚠️ L'AC4 SERA LIVRÉ À MOITIÉ, ET C'EST UNE DÉCISION, PAS UN OUBLI.
                          # « Cases lisibles » : oui. « Directement actionnables » : non —
                          # assigner une recette EST la story 3.6, et poser d'ici là une case
                          # focalisable qui ne mène nulle part serait la « zone ambiguë » que le
                          # même critère interdit. Option (a) retenue : la sous-tâche reste
                          # DÉCOCHÉE avec sa raison, et la 3.6 doit la ROUVRIR en la citant
                          # (règle §5), pas la supposer close. Règle §1 côté critères : une case
                          # vide honnête vaut mieux qu'une case cochée à tort.
                          # Aussi tranché : quatre repas (petit-déj · midi · collation · soir),
                          # et la semaine en cours par défaut.
                          # Aucune migration due : `meal_plan_entries`, sa RLS et ses types
                          # existent depuis le squelette du 2026-05-02.
last_updated: 2026-08-03  # 2026-08-03 — Parcours à l'écran de la story 3.3 REJOUÉ EN ENTIER après
                          # les correctifs de la revue. Rapporté par Florian, pas mesuré par
                          # l'agent (règle §1). Story `in-progress` → `review`.
                          # Il ferme les deux points qu'aucune porte automatique ne pouvait
                          # fermer : le repli du squelette (déduit → observé) et le correctif
                          # `{quantite !== null}`, que la mutation avait montré sans dent.
                          # ⚠️ La PR #20 (microcopy) fusionne AVANT la #19.
last_updated: 2026-08-02  # 2026-08-02 — REVUE ADVERSARIALE de la story 3.3, la première depuis la
                          # 2.1. Elle rompt la série de trois fusions sans revue (2.2, 3.1, 3.2)
                          # que ce fichier signalait comme « le dernier filet n'est plus tendu ».
                          # Détail à l'entrée de la story 3.3. Deux branches en sortent :
                          # `feat/recettes-lecture` (PR #19, story seule) et
                          # `refactor/microcopy-possessifs` (passe microcopy extraite).
                          # ⚠️ La microcopy fusionne EN PREMIER.
                          # Story 3.3 : `review` → `in-progress`, le parcours à l'écran étant à
                          # rejouer en entier.
last_updated: 2026-08-01  # 2026-08-01 — Story 3.1 contextualisée puis EN COURS D'IMPLÉMENTATION,
                          # hors de l'ordre du sprint : l'Epic 2 n'est pas clos (2.2 en revue dans
                          # la PR #15, 2.3 et 2.4 en backlog). Choix visible, pas un oubli —
                          # l'Epic 3 ne dépend d'aucune des deux. Détail à l'entrée de la story.
                          #
                          # Le conflit annoncé avec la PR #15 a bien eu lieu, sur ce bloc, sur la
                          # ligne de la story 2.2 et sur `isolation.test.ts` — résolu le
                          # 2026-08-02 en gardant LES DEUX côtés à chaque fois, la #15 ayant été
                          # fusionnée en premier (sa migration ne crée qu'une fonction, donc sans
                          # risque sur les données ; celle de la #17 pose deux contraintes `check`).
last_updated: 2026-07-31  # Story 2.2 implémentée, en attente de revue (branche
                          # `feat/rayons-reordonner-parcours`, depuis `f29c1a1`). Cinq portes
                          # vertes : 96/96 unitaires, 27/27 isolation, dents vérifiées (27 → 25
                          # sans la garde de comptage). Le parcours à l'écran a trouvé un défaut
                          # qu'aucune porte ne voyait : en thème sombre la ligne tirée était
                          # TRANSPARENTE (`--surface-card` = 5 % de blanc, `--card-shadow` =
                          # `none`), deux noms superposés. Corrigé, revérifié dans les deux thèmes.
                          # Latence tranchée par la mesure : 81 ms médians sur le BUILD DE
                          # PRODUCTION contre ~1000 ms en développement — pas de mise à jour
                          # optimiste, la question 2 est close.
                          # ⚠️ RESTE AVANT FUSION : le glisser AU DOIGT (joué à la souris seulement ;
                          # `touch-action: none` est mesuré, le geste ne l'est pas), la requête de
                          # contrôle de migration sur la production, et les questions du gabarit de PR.
                          #
                          # 2026-07-30 — Story 2.2 contextualisée. Détail au niveau de son entrée ci-dessous.
                          #
                          # 2026-07-29 — Revue adversariale de la story 2.1. Elle a trouvé, hors du périmètre de
                          # la story, que `main` n'était pas protégée : depuis `vercel.json`, un
                          # commit sur `main` applique les migrations en production, gardé par le
                          # seul `next build`. Protection posée, job CI `isolation` ajouté — les 17
                          # tests d'isolation ne tournaient dans aucun automate.
                          #
                          # Story 2.1 contextualisée ; Epic 2 ouvert. Sa création a mesuré un trou
                          # d'isolation dans `seed_default_aisles` (security definer à household_id
                          # en paramètre, sans contrôle) : un membre peut écrire dans les rayons d'un
                          # autre foyer. Correctif prescrit et éprouvé en local, porté par la story.
project: nutriclaude
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # ── Epic 1 : Socle technique & foyer partageable ──
  epic-1: done
  1-1-poser-le-socle-applicatif-next-16: done
  1-2-authentification-par-magic-link-sans-mot-de-passe: done
  1-3-creer-un-foyer-a-l-inscription: done
  1-4-generer-un-code-d-invitation: done
  1-5-rejoindre-un-foyer-via-un-code-d-invitation: done
  1-6-ecran-profil-membres-du-foyer: done
  # Close sur décision de Florian (2026-07-28). Les deux contrôles alors ouverts
  # ont été menés le 2026-07-29 ; il en reste un morceau :
  #   1. relecture visuelle : SIX écrans sur sept vus dans les DEUX thèmes. Elle a
  #      trouvé trois défauts qu'aucune des quatre portes ne voyait, dont deux
  #      consignés « traités » par la passe 3 sans l'être. /onboarding reste à
  #      voir : il exige un compte sans profil, donc l'environnement de test.
  #   2. `20260728152418_require_non_blank_household_name.sql` : appliquée.
  1-7-fondations-de-theme-clair-sombre-de-ton: done
  epic-1-retrospective: done

  # ── Epic 2 : Les rayons — les rendre visibles, modifiables et ordonnables ──
  # Révisé le 2026-07-29 : 7 stories → 4. Constat et arbitrages dans
  # `planning-artifacts/epic-2-revision-2026-07-29.md`.
  #   · l'ancienne 2.1 « Amorcer un jeu de rayons » est SUPPRIMÉE — 3 de ses 4
  #     critères sont tenus en production depuis le squelette ; son critère
  #     restant est absorbé par la nouvelle 2.1.
  #   · les anciennes 2.5, 2.6, 2.7 sont DÉPLACÉES en Epic 4 (4.16 à 4.18) :
  #     leurs critères s'énoncent sur la liste de courses, donc indémontrables ici.
  epic-2: in-progress
  # DEUX passes de revue adversariale le 2026-07-29. La première : 5 décisions, 14
  # correctifs, 6 reportés — son fait marquant était hors périmètre (`main` non protégée,
  # migrations de production gardées par `next build` seul, tests d'isolation dans aucun
  # automate). La seconde a porté sur le commit de correction de la première : celle-ci
  # avait réparé la moitié d'un défaut, en avait introduit trois, et affirmé deux choses
  # fausses. 9 correctifs de plus.
  #
  # Les trois vérifications qui retenaient la story sont fermées (2026-07-29/30) :
  # parcours à l'écran dans les deux thèmes, job CI `isolation` sur un runner, état vide
  # en sombre observé. La portée de SUPABASE_DB_URL est confirmée par Florian.
  2-1-gerer-ses-rayons: done
  # Contextualisée le 2026-07-30. Le mécanisme a été ÉPROUVÉ sur le stack local avant
  # d'être prescrit — huit contrôles, deux comptes, base remise à l'état du dépôt :
  # fonction `reorder_aisles(uuid[])` en `security invoker` (le contraire exact de
  # `seed_default_aisles`, dont la story 2.1 a dû refermer le trou), quatre gardes, et
  # un renumérotage complet qui RÉSORBE les ex æquo (1 position distincte → 11).
  # Le seul contrôle qui attrape un appel forgé à cardinal exact est le comptage de
  # lignes après l'`update` : sous RLS, écrire sur une ligne masquée ne rend AUCUNE
  # erreur. La sonde a aussi trouvé deux faux positifs de test — sous RLS, A ne peut
  # pas lire les identifiants de B, donc un test inter-foyers doit les obtenir du
  # client `admin`, sans quoi il passe en ne prouvant rien.
  #
  # Les 4 questions ont été TRANCHÉES par Florian le 2026-07-30, avant démarrage. La
  # première contre la recommandation : le GLISSER entre dans le périmètre, en plus des
  # flèches. Fait main sur événements de pointeur — `draggable` HTML5 n'émet rien au
  # toucher sur iOS, l'appareil de référence de NFR-1. ⚠️ Les flèches restent
  # obligatoires : WCAG 2.5.7 exige une alternative à pointeur simple à tout mouvement
  # de glissement, et elles sont le seul chemin clavier (UX-DR11). Ordre de construction
  # imposé — le chemin flèches en entier, puis le glisser par-dessus le même primitif pur.
  # Coût dit d'avance : ~200 lignes de JSX qu'aucun test ne couvre (NFR-10), et une
  # vérification manuelle DÉDOUBLÉE souris/doigt.
  # Décisions 2 à 4 conformes aux prescriptions : pas de mise à jour optimiste mais le
  # ressenti doit être MESURÉ et rapporté ; pas de contrainte d'unicité sur `sort_order`
  # (l'invariant se mesure par test) ; une 4e région de statut sous la liste.
  # ⚠️ FUSIONNÉE le 2026-08-02 (PR #15, squash `86fafe7`), déploiement de production
  # RÉUSSI — `reorder_aisles` est appliquée en production. **Sans revue adversariale** :
  # passée de `review` à fusionnée sur décision de Florian, alors que la règle n°6 de
  # `project-context.md` en demande une par story. À rattraper ou à acter.
  # ⚠️ RESTE NON VÉRIFIÉ, ET C'EST MAINTENANT EN PRODUCTION : le glisser AU DOIGT n'a
  # jamais été joué (souris seulement ; `touch-action: none` est mesuré, le geste ne
  # l'est pas). C'est la classe d'erreur qui a fait écarter `draggable` HTML5.
  2-2-reordonner-le-parcours-par-manipulation-directe: done
  2-3-definir-des-regles-mot-cle-rayon: backlog
  # Contextualisée le 2026-08-06, HORS ORDRE : elle passe devant la 2.3 parce que la décision D1
  # de la story 4.2 (Florian, 2026-08-06) lui confie le composant carte-rayon, et que la 4.2 est
  # ARRÊTÉE en attendant. C'est la plus petite story du dépôt, et la plus inhabituelle : elle
  # livre un composant React de présentation, SANS écran et SANS consommateur.
  # ⛔ ONZE MESURES, et trois invalident le cadrage :
  #   · LE DÉPÔT N'A AUCUN MOYEN D'ÉPROUVER UN COMPOSANT. `npm test` ne globe que
  #     `lib/**/*.test.ts`, et les 22 fichiers de test portent tous sur des fonctions pures ou sur
  #     la base. NFR-10 interdit d'ajouter un harnais. Or l'AC3 dit « il est éprouvé sans liste ni
  #     base » — sans décision, ce critère est un vœu. C'est la décision D1.
  #   · LA PRÉMISSE QUI A GARDÉ CETTE STORY DANS L'EPIC 2 EST PÉRIMÉE.
  #     `epic-2-revision-2026-07-29.md` §5-D2 la justifie par « il est bâtissable et VISIBLE SUR
  #     L'ÉCRAN DES RAYONS » — écrit AVANT que cet écran existe. Celui que la story 2.1 a construit
  #     rend des LIGNES avec poignée de glisser, pas des cartes, et `app/rayons/page.tsx:20-23`
  #     exclut explicitement la carte-rayon. Règle §5 : la prémisse se rouvre.
  #   · LE TOKEN `--text-qty` QUE LE RATIO EXIGE N'EXISTE PAS (`DESIGN.md:278` le prescrit).
  # ⚠️ ELLE LIVRE UNE CARTE, ET LA CARTE EST UN PIÈGE EN THÈME SOMBRE : mesuré,
  # `--card-shadow: none` et `--surface-card` à 5,5 % de blanc — LA BORDURE EST LE SEUL SÉPARATEUR.
  # C'est exactement ce qui a rendu la ligne tirée invisible à la story 2.2 (« cinq portes vertes,
  # zéro signal — le troisième epic d'affilée où le défaut décisif est trouvé par l'œil »).
  # ⚠️ Et c'est une story presque entièrement hors de portée des tests : le motif de `86fafe7`
  # (2.2 fusionnée sans revue, six défauts d'usage ensuite, TOUS dans du JSX qu'aucun test
  # n'exécute) s'applique mot pour mot.
  # ⛔ DEUX DÉCISIONS OUVERTES, chacune avec son défaut prescrit : D1 comment la story se démontre
  # (logique pure extraite dans `lib/rayons/carte.ts` + preuve visuelle reportée à la 4.2, ou
  # montage sur `/rayons` qui toucherait la story 2.2), D2 le contrat de props (`id` pour la 4.18,
  # `pris` optionnel, `children`).
  # ✅ IMPLÉMENTÉE le 2026-08-06 (statut `review`). Trois fichiers, aucune dépendance, aucune
  # migration : `lib/rayons/carte.ts` (+ 9 tests), `app/_lib/CarteRayon.tsx`, et le token
  # `--text-qty`. D1 et D2 n'ayant pas été tranchées, les DÉFAUTS PRESCRITS ont été appliqués.
  # ⚠️ À l'implémentation, l'AC3 n'était tenu QU'À MOITIÉ, et c'était écrit plutôt que maquillé :
  # le rendu n'était éprouvé par rien, aucun écran ne montant le composant.
  # ✅ REVUE ADVERSARIALE À QUATRE COUCHES PUIS FERMÉE (`done`) le 2026-08-07. 27 constats :
  # 3 décisions tranchées par Florian, 19 correctifs appliqués, 5 reportés, 5 écartés.
  # ⛔ LES DEUX DÉFAUTS MAJEURS ÉTAIENT DANS LE JSX QU'AUCUNE PORTE N'EXÉCUTE — là où la story
  # avait prédit qu'ils seraient :
  #   (1) l'`aria-label` du ratio était posé sur un `<span>` NU (rôle `generic`, name-prohibited
  #       en ARIA 1.2) : tout le type `RatioDeRayon`, testé et documenté trois fois, alimentait un
  #       attribut qui n'atteignait AUCUN lecteur d'écran. Le dépôt avait déjà payé cette leçon
  #       sur `InviteCard` et écrit le remède (`.sr-only`) ;
  #   (2) le « banc des dents » annonçait quatre passages propres — MESURÉ, l'un était FAUX. Le
  #       mutant qui reconstruit l'annonce à part du visible laisse la suite entière verte, et
  #       aucun test ne peut l'attraper. `libelleRatio` dérive désormais `pourLecteur` de
  #       `visible` : l'invariant est tenu PAR CONSTRUCTION.
  # ⚠️ TROIS COMMENTAIRES AFFIRMAIENT UN ÉTAT DE LA BASE, ET LES TROIS ÉTAIENT FAUX (règle §2) :
  # « seed_default_aisles couvre les onze rayons » (« À classer » n'a pas de ligne `aisles`),
  # « muted-2 est à 2,46:1 » (valeur d'avant l'arbitrage, puisée dans le document que
  # `globals.css:5-9` interdit de citer), « déstructurer `id` ferait échouer lint »
  # (`eslint.config.mjs` pose `^_`). AUCUN n'était détectable par une porte automatique.
  # ✅ AC3 TENU : Florian a tranché pour une ROUTE JETABLE plutôt que le report. Sept cartes
  # parcourues aux DEUX RÉGLAGES SYSTÈME, à 390 px et en large, puis route supprimée avant le
  # commit (`git diff` vide sur `proxy.ts`).
  # ⚠️ CE QUI RESTE OUVERT, DATÉ PLUTÔT QU'EFFACÉ (§6 bis) : le composant n'a toujours AUCUN
  # appelant en production. Le voir EN SITUATION — vraie liste, vraies données, sous un vrai
  # `<h1>` — reste à la story 4.2. Et entre les deux, aucune porte ne garde ce JSX.
  # ✅ Portes : npm test 216/216 (+18 sur la story) · isolation 95/95 · lint · typecheck ·
  # check:migrations 16/14/2/0 · build · 23 utilitaires sur 23 générés, contrôle négatif OK.
  # ✅ `project-context.md` corrigé dans le même commit : l'interdit sur l'abricot est BORNÉ, pas
  # effacé — cette story pose le premier abricot du produit (la pastille de la carte-rayon).
  2-4-composant-carte-rayon: done
  epic-2-retrospective: optional

  # ── Epic 3 : Recettes & menu — les affluents de la liste ──
  epic-3: in-progress
  # Contextualisée le 2026-08-01, les 5 questions tranchées par Florian, périmètre dégonflé
  # d'une route, puis implémentation lancée le jour même. Les deux contraintes prescrites ont
  # été ÉPROUVÉES sur le stack local AVANT d'être écrites — huit contrôles SQL, deux appels
  # PostgREST, base remise à l'état du dépôt.
  #
  # ⚠️ LE PIÈGE CENTRAL, MESURÉ ET NON DÉDUIT : `normaliserTexte` de `lib/texte.ts` DÉTRUIT
  # tous les retours à la ligne — `INVISIBLES` contient `\p{Cc}`, qui comprend U+000A.
  # `instructions` est le PREMIER champ multiligne du produit ; sans un normalisateur distinct,
  # l'AC de la story 3.3 (« la mise en forme est préservée à la lecture ») serait indémontrable,
  # la mise en forme ayant été détruite à l'écriture deux stories plus tôt. Aucun signal.
  #
  # Deux décisions CONTRE la recommandation initiale de la story : la suppression d'une recette
  # entre au périmètre (créer sans supprimer est un aller simple ; /rayons supprime déjà ; et la
  # cascade sur `meal_plan_entries` n'a rien à avertir tant que le menu n'existe pas), et la
  # garde des saisies non enregistrées aussi. Le bouton Retour du navigateur reste
  # DÉLIBÉRÉMENT découvert — la parade casserait le contrat du navigateur.
  #
  # ⚠️ Périmètre dégonflé : la création se fait AU TITRE SEUL depuis le répertoire, puis continue
  # sur l'écran d'édition. Conséquence assumée : AC1 se lit EN DEUX TEMPS, signalé sous les AC.
  #
  # 2026-08-01 — IMPLÉMENTÉE, en attente de revue (branche `feat/recettes-creer-editer`, depuis
  # `2ad08c4` = main). SIX portes vertes : 103/103 unitaires, 34/34 isolation, build, typage,
  # lint, en-têtes de migration. Dents vérifiées par mutation (34 → 32 → 30 ; 103 → 100).
  # ⚠️ Une 4e mutation n'a rien fait tomber, et c'était MOI qui avais tort : pour une politique
  # `FOR ALL`, Postgres réutilise `using` comme `with check` quand celle-ci est omise. Rejouée
  # en `with check (true)`, un seul test tombe — le bon.
  # Le piège central mesuré de bout en bout : instructions sur 3 lignes → 3 sauts conservés en
  # base. Une prédiction de la story CONFIRMÉE (« 2e3 » passe la validation native de Chrome et
  # atteint `normaliserEntier`), une DÉMENTIE (les refus « portions » sont inatteignables depuis
  # l'écran, `required` et `min={1}` bloquent avant — la contrainte en base reste la frontière).
  # ⚠️ RESTE AVANT FUSION : la garde sur ses 3 autres chemins (seul le clic sur le lien de retour
  # a été joué ; le bouton Retour du navigateur n'est couvert par RIEN, délibérément), le
  # `<textarea>` sur un texte long, la requête de contrôle de migration sur la PRODUCTION
  # (attendu zéro ligne — DÉDUIT, pas mesuré), et les questions du gabarit de PR.
  # ⚠️ Piège d'outillage à connaître : après `supabase db reset`, Kong garde l'ancienne adresse
  # du conteneur d'authentification — `AuthRetryableFetchError`, 25 tests rouges, /auth/v1/health
  # en 502. `docker restart supabase_kong_nutriclaude` et tout revient. Ça ressemble à une
  # régression et ça n'en est pas une.
  # Une action de l'Epic 1 se ferme au passage : /onboarding vu en thème CLAIR.
  # ⚠️ FUSIONNÉE le 2026-08-02 (PR #17, squash `d270c47`), déploiement de production
  # RÉUSSI. **Ce succès PROUVE ce que la requête de contrôle devait établir** : les deux
  # contraintes `check` se sont appliquées sans échouer, donc aucune ligne de `recipes`
  # ne les violait en production. Preuve plus forte que la requête elle-même — mais
  # obtenue APRÈS coup, pas avant, ce que le gabarit de PR demandait.
  # ⚠️ **Sans revue adversariale**, comme la 2.2 (règle n°6 de `project-context.md`).
  # ⚠️ RESTE NON VÉRIFIÉ, EN PRODUCTION : la garde des saisies non enregistrées sur 3 de
  # ses 4 chemins (seul le clic sur le lien de retour a été joué ; `beforeunload` est posé
  # et typé mais non observé ; le bouton Retour du navigateur n'est couvert par RIEN,
  # délibérément), et le `<textarea>` sur un texte long (éprouvé sur 4 lignes, pas 30).
  3-1-creer-et-editer-une-recette: done
  # Contextualisée le 2026-08-02. Le mécanisme de réordonnancement a été ÉPROUVÉ PAR
  # EXÉCUTION avant d'être prescrit, et la sonde a trouvé un trou que la story 2.2 n'avait
  # PAS : calquée à l'identique sur `reorder_aisles`, la fonction accepte un appel annonçant
  # une recette et citant les identifiants d'une AUTRE RECETTE DU MÊME FOYER — et la
  # renumérote. Les quatre gardes ne le voient pas : la RLS est par foyer, la portée de la
  # garde est la recette, et les deux ne coïncident plus (elles coïncidaient pour les rayons).
  # Correctif éprouvé à son tour : le filtre `and ri.recipe_id = p_recipe_id` DANS l'update.
  #
  # ⚠️ LE VOCABULAIRE D'UNITÉS EST UN CONTRAT AVEC L'EPIC 4, et il n'existe nulle part :
  # `unit` est un `text` sans aucune contrainte, et le commentaire du schéma (`:162`)
  # CONTREDIT AD-7 — cinq jetons ASCII (« piece ») contre huit accentués (« pièce »).
  # `generate_grocery_list_from_menu` groupe par `ri.unit` brut et le recopie dans
  # `grocery_list_items.unit`, qui est un morceau de la clé canonique d'AD-3. Mesuré :
  # « pièce » en NFD est une chaîne DIFFÉRENTE, et Postgres les juge inégales — deux lignes
  # de courses qui ne fusionneraient jamais. D'où `<select>` et jamais de saisie libre.
  #
  # Mesuré aussi : `sort_order` vaut 0 par défaut pour TOUS les ingrédients — les ex æquo
  # sont ici l'état de départ, pas un cas limite comme sur les rayons.
  #
  # ⚠️ DÉPENDANCE DE BRANCHE, première question ouverte : `main` n'a NI la 2.2 (PR #15) NI
  # la 3.1 (PR #17). Cette story a besoin de l'écran de la 3.1 et du module d'ordre de la
  # 2.2 — qui est déjà générique et doit être EXTRAIT en `lib/ordre.ts`, pas recopié.
  # 5 questions ouvertes (branche, suppression d'un ingrédient, glisser ou flèches seules,
  # contrainte sur la quantité, sort du module extrait).
  # ⚠️ FUSIONNÉE le 2026-08-02 (PR #18, squash `8f91f52`), déploiement de production RÉUSSI.
  # ⚠️ **CE QUE CE SUCCÈS PROUVE, ET CE QU'IL NE PROUVE PAS** — la revue du 2026-08-03 a
  # mesuré que la formule employée pour la 3.1 ne se transpose PAS ici. Elle vaut pour la
  # migration des trois `check` : un `add constraint` échoue si une ligne viole, donc son
  # application prouve qu'aucune ligne de `recipe_ingredients` ne les violait. Elle NE vaut
  # PAS pour `20260802112749` : un `create or replace function` **ne peut pas** échouer sur
  # des données — l'en-tête de la migration le dit lui-même — donc sa requête d'ex æquo n'a
  # TOUJOURS rien établi, et ne le pourra pas rétroactivement. Écrit ici plutôt que corrigé
  # en silence : c'est la règle §5, une prémisse se rouvre au lieu d'être retirée.
  #
  # ✅ REVUE ADVERSARIALE MENÉE le 2026-08-03, la première pour cette story et APRÈS sa mise
  # en production. 4 décisions, 19 correctifs, 6 reports. Le cœur technique est SOLIDE et
  # honnêtement mesuré — le trou inter-recettes était réel, le correctif tient (mutation
  # rejouée sur stack local : 55 → 54, et le seul test tombé est le bon), et l'accord
  # `UNITES` ↔ contrainte est le meilleur exemple de règle §4 du dépôt.
  # ⚠️ LE MOTIF, en revanche : la story a mesuré ce qu'elle avait PRÉVU de mesurer, et rien
  # d'autre. Les six défauts d'usage sont TOUS dans les 660 lignes de JSX qu'aucun test
  # n'exécute (NFR-10) — et le fichier documente la leçon qu'il enfreint : son commentaire
  # nomme cette famille « la cinquième fois », corrige le chemin de suppression, et la
  # reproduit sur les quatre chemins voisins.
  # ⚠️ Et `recipe_ingredients_nom_non_vide` n'avait AUCUNE dent : retirer la contrainte
  # laissait 55/55. La Task 8 affirmait pourtant les avoir passées au banc des dents.
  # Correctifs dans la branche `fix/ingredients-revue-3-2` (PR à part : le code est en
  # production, sa fermeture n'attend pas ces correctifs).
  # ⚠️ REPORTÉ à la story 4.10, décision de Florian : l'édition concurrente écrase
  # silencieusement celle de l'autre membre et affiche « C'est noté. ».
  # ⚠️ NON LIVRÉ, et c'est un choix : le GLISSER. Les flèches suffisent aux critères et sont
  # obligatoires dans les deux mondes (WCAG 2.5.7 + seul chemin clavier), donc rien n'est à
  # jeter s'il est repris — il se poserait sur `lib/ordre.ts`, qui porte déjà
  # `indexCibleDuGlisser`.
  3-2-gerer-les-ingredients-d-une-recette: done
  # Contextualisée le 2026-08-02. Aucune migration — première story de l'Epic 3 dans ce cas.
  # LA MOITIÉ D'AC2 EST DÉJÀ TENUE : `normaliserMultiligne` (story 3.1) stocke les retours à
  # la ligne, et la sonde confirme qu'ils survivent à l'aller-retour par l'API, ligne vide
  # intérieure comprise. Il ne reste qu'un `whitespace-pre-wrap` — et le piège n°1 énumère
  # les quatre façons de le rater, dont `dangerouslySetInnerHTML`, qui ouvrirait une vraie
  # surface XSS sur un produit SANS CSP.
  #
  # ⚠️ C'EST CETTE STORY QUI ROUVRE LA CSP. Son échéance a été repoussée à l'Epic 6 le
  # 2026-08-01 au motif que « cet epic n'a pas ouvert de surface XSS ». L'affirmation portait
  # sur l'ÉCRITURE ; celle-ci est la story qui LIT du texte écrit par le membre. D'où une
  # tâche sans critère d'acceptation, et qui n'est pas facultative : revérifier la prémisse
  # et écrire la conclusion (règle §5 de project-context.md).
  #
  # ⚠️ Un défaut MESURÉ, hérité de la 3.2 : PostgREST rend `quantity` en nombre JSON, donc
  # « 0,5 » saisi se réaffiche « 0.5 » — alors que `normaliserQuantite` accepte explicitement
  # la virgule française et que NFR-8 exige le français. Corrigé dans l'écran de lecture
  # seulement ; consigné dans deferred-work pour l'écran d'édition, pour ne pas déborder.
  #
  # 4 questions ouvertes (lien de retour de l'édition, generateMetadata, un temps de cuisson
  # à 0, un loading.tsx pour cet écran), chacune avec sa recommandation.
  # Les 4 questions TRANCHÉES par Florian le 2026-08-02, conformes aux recommandations :
  # lien de retour de l'édition inchangé, generateMetadata oui, un temps à 0 S'AFFICHE
  # (distinct de null), et AUCUN loading.tsx sur cette route — absence décidée, pas subie.
  # ⚠️ REVUE ADVERSARIALE MENÉE le 2026-08-02 — la PREMIÈRE depuis la 2.1, après trois
  # stories fusionnées sans (2.2, 3.1, 3.2). Trois couches sans contexte préalable sur
  # `8f91f52..cfcc75e`. 4 décisions, 9 correctifs, 5 reports, 3 écartés.
  # AC1, AC2 et AC3 sont tenus PAR LE CODE et les huit pièges sont évités ; les défauts
  # étaient dans ce que la story AFFIRMAIT et dans son second commit.
  # LE FAIT MARQUANT, MESURÉ ET NON DÉDUIT : `app/recettes/loading.tsx` COUVRE bien
  # `/recettes/[id]` — aucun `layout.tsx` sous `app/recettes/`, donc le `loading.tsx` du
  # segment enveloppe tous ses enfants, et le build le confirme. La décision 4 (« aucun
  # loading.tsx, absence décidée ») reposait donc sur une prémisse fausse écrite en trois
  # endroits : réseau lent, le membre voyait le squelette DU RÉPERTOIRE avant sa recette.
  # `app/recettes/[id]/loading.tsx` posé sur décision de Florian.
  # ⚠️ La passe microcopy a été EXTRAITE dans `refactor/microcopy-possessifs` (PR à part) :
  # elle touchait 11 fichiers hors périmètre ET l'écran de la story APRÈS que son parcours
  # ait été consigné — les preuves attestaient d'un état que HEAD n'était plus.
  # ⚠️ ORDRE DE FUSION IMPOSÉ : la PR microcopy passe AVANT la #19.
  # ⚠️ Cinq cases DÉCOCHÉES avec leur raison (règle §1) : phase rouge TDD, les deux thèmes,
  # le focus dans le DOM, les trous un par un, le SHA-256 de `.env.local`. Tasks 1, 3 et 5
  # décochées avec elles — la 5 EN ENTIER.
  # ⚠️ La revue s'est trompée une fois, et c'est consigné : « ← Retour » signalé sur trois
  # écrans alors que le code portait sa justification à six lignes du constat. Règle §6 —
  # la passe de correction doit être revue à son tour.
  # ✅ 2026-08-03 — PARCOURS À L'ÉCRAN REJOUÉ EN ENTIER sur HEAD, après les correctifs.
  # RAPPORTÉ PAR FLORIAN, pas mesuré par l'agent (règle §1). Les quatre points qui le
  # rendaient nécessaire sont couverts : les deux thèmes remis après, le RÉSEAU BRIDÉ sur le
  # squelette neuf, un ingrédient à QUANTITÉ 0 et un à UNITÉ SANS QUANTITÉ, le zoom 200 % /
  # 390 px / focus dans le DOM. Statut `in-progress` → `review`.
  # Deux choses que ce parcours ferme, et qu'aucune porte ne pouvait fermer :
  #   · le repli du squelette n'est plus DÉDUIT — c'est bien celui de la FICHE qui s'affiche,
  #     pas celui du répertoire ;
  #   · le correctif `{quantite !== null}`, dont la mutation avait montré qu'AUCUN test ne le
  #     couvre (NFR-10), a eu son seul filet possible.
  # ⚠️ TROIS CASES RESTENT VIDES et le resteront : la phase rouge du TDD (une preuve du passé
  # ne se fabrique pas), le SHA-256 de `.env.local` (non rapporté, non inféré d'un parcours
  # réussi), les trous « un par un ». Elles ne bloquent pas la fusion.
  # ⚠️ RESTE AVANT FUSION : la PR #20 (microcopy) passe AVANT la #19.
  3-3-consulter-une-recette-en-lecture: review
  # ⚠️ SAUTÉE À LA DEMANDE DE FLORIAN le 2026-08-04 : la 3.5 a été contextualisée avant elle.
  # Elle reste due — c'est elle qui apporte les étiquettes que la maquette du menu affiche.
  3-4-etiquettes-filtre-et-recherche-du-repertoire: backlog
  # 2026-08-04 — Contextualisée, puis ses trois questions tranchées avant démarrage.
  # Grille jour × repas EN LECTURE (quatre repas : petit-déj · midi · collation · soir),
  # navigation entre semaines, semaine en cours par défaut, case vide lisible.
  # Assigner une recette reste la 3.6 en entier.
  # ⚠️ L'AC4 sera livré à moitié, par décision datée — voir le fichier de story.
  # 2026-08-04 — Implémentée sur `feat/menu-grille` (branchée sur 795678e). Six portes vertes :
  # 179/179 unitaires (159 avant), 60/60 isolation (57 avant), dents mesurées par mutation sur
  # les deux suites. Aucune migration due.
  # ⛔ NE PAS FUSIONNER EN L'ÉTAT : le parcours à l'écran (Task 5) N'A PAS EU LIEU, et l'AC2
  # — « aucun défilement horizontal », le critère central de cette story — n'est donc PAS
  # démontré. Ce qui est établi l'est par mesure STATIQUE (toutes les classes émettent bien
  # du CSS, les colonnes sont en minmax(0,1fr), les titres portent break-words) : c'est un
  # raisonnement solide, pas une observation. Règle §7 — le rendu est ce qu'aucune porte ne voit.
  # ⚠️ Trouvé à l'implémentation : `meal_plan_all` ne contrôle pas la provenance de
  # `recipe_id` — un foyer peut pointer la recette d'un autre. Ce n'est PAS une fuite (la RLS
  # filtre la ressource jointe, mesuré) mais un défaut d'intégrité, à fermer par la story 3.6
  # en même temps que la contrainte d'unicité d'AD-6. Détail dans `deferred-work.md`.
  3-5-planifier-le-menu-de-la-semaine-sans-defilement-horizontal: review
  # 2026-08-04 — Contextualisée sur `a56ba0b`. Dernière story de l'Epic 3, PREMIÈRE écriture
  # dans `meal_plan_entries`, et la seule de l'epic à porter une migration depuis la 3.2.
  # Elle REFERME trois choses laissées ouvertes, chacune citée et non résumée :
  #   1. la moitié d'AC4 de la 3.5 (« directement actionnables ») — la case a été dessinée,
  #      dimensionnée et nommée pour ça ; il ne lui manque que sa destination ;
  #   2. le trou de provenance de `recipe_id`, MESURÉ le 2026-08-04 (A peut pointer la recette
  #      de B ; ce n'est pas une fuite — la RLS filtre la ressource jointe — mais une intégrité
  #      qui manque). Sa fermeture vit dans la MÊME migration que l'unicité d'AD-6 ;
  #   3. l'AC2 de la 3.5, fusionné sans jamais avoir été observé.
  # ✅ 4 QUESTIONS TRANCHÉES LE 2026-08-04, avant démarrage — détail dans le fichier de story.
  # La 4e est sortie des options proposées et ÉLARGIT LE PÉRIMÈTRE : le nombre de personnes se
  # règle au FOYER (`households.default_servings`, colonne neuve, section neuve sur `/foyer`,
  # première écriture du produit dans `households`) puis s'ajuste par assignation.
  # Conséquence : `supabase gen types` est DÛ — la forme du schéma change, ce qui n'aurait pas
  # été le cas avec les seules contraintes. 10 tâches, toutes ouvertes.
  # 2026-08-04 — IMPLÉMENTÉE sur `feat/menu-assignation` (branchée sur `a56ba0b`).
  # 11 fichiers créés, 13 modifiés. Une migration à QUATRE volets : unicité d'AD-6, provenance
  # de `recipe_id` (politique resserrée), `servings > 0`, et `households.default_servings`.
  # 197/197 unitaires (179 avant), 66/66 isolation+contraintes (60 avant). 8 mutations jouées,
  # chacune faisant tomber le test qu'elle vise ET LUI SEUL. Six portes vertes.
  # ✅ PARCOURS À L'ÉCRAN JOUÉ EN ENTIER, et il a fermé DEUX dettes d'un coup : les 4 AC de
  # cette story, la moitié d'AC4 que la 3.5 avait laissée, ET L'AC2 DE LA 3.5 — « aucun
  # défilement horizontal » — qui était en production sans avoir jamais été observé.
  # ⚠️ IL A TROUVÉ 2 DÉFAUTS QU'AUCUNE DES SIX PORTES NE VOYAIT (règle §7, deux fois) :
  #   · `min={1}`/`required` faisaient afficher un message de navigateur EN ANGLAIS, hors
  #     région aria-live — et empêchaient le gestionnaire d'être appelé, donc rendaient DEUX
  #     messages français INATTEIGNABLES (des clés de message sans appelant) ;
  #   · « et des 2repas » — JSX mangeait l'espace après un `<span>`.
  #   Les deux corrigés et REVÉRIFIÉS à l'écran.
  # ⚠️ `estCodeRepas` n'a PAS été écrit, et c'est délibéré : la route reçoit un slug, pas un
  # code, donc le prédicat prescrit n'aurait eu aucun appelant. `repasParSlug` tient le rôle.
  # ⚠️ Le CLI Supabase de ce poste régénère `types.ts` en PERDANT `__InternalSupabase`, qui
  # pilote l'inférence de types côté client. Le delta a été appliqué au lieu d'écraser, et
  # l'écart est MESURÉ par `diff`, pas affirmé.
  # ⛔ RESTE AVANT FUSION, et ce n'est pas un oubli : la requête de contrôle de la migration
  # sur la PRODUCTION (c'est le geste de la revue de PR, et la fusion APPLIQUE la migration),
  # et « le dire dans la PR » que l'AC2 n'y est pas démontrable. Deux cases décochées avec
  # leur raison ; une troisième porte l'écart de méthode sur la mesure de largeur.
  # 2026-08-05 — REVUE ADVERSARIALE menée (3 couches en contexte vierge) + passe de correction.
  # 21 findings, 7 levés par au moins deux couches. 14 correctifs appliqués, 6 reports datés.
  # ⚠️ Menée par le modèle QUI A IMPLÉMENTÉ : la règle §6 demande un autre modèle, et les
  # sous-agents à contexte vierge sont une atténuation, pas un équivalent.
  # 2 décisions de Florian : décocher le refus `23514` (inatteignable), et REFONDRE
  # `AssignerRepas` en « une ligne ouverte à la fois » — refonte qui referme 4 findings.
  # ⚠️ 4 AFFIRMATIONS FAUSSES du Dev Agent Record corrigées, dont `loading.tsx` déclaré
  # « intact » alors qu'il ne suivait plus la hauteur des cases (saut de mise en page sur 28
  # cases), et une empreinte SHA-256 de `.env.local` qui ne prouvait rien — le fichier porte un
  # jeton Vercel de 12 h que la CLI réécrit seule.
  # ✅ 2026-08-05 — Requête de contrôle EXÉCUTÉE SUR LA PRODUCTION, zéro ligne aux trois volets.
  #    ⚠️ RAPPORTÉ PAR FLORIAN, pas mesuré par l'agent (règle §1) : l'URI de production avait
  #    disparu de `.env.local`, emportée par la réécriture de la CLI Vercel. Condition de
  #    fusion levée. Statut `in-progress` → `review`, puis fusion demandée par Florian.
  # ⛔ RESTE OUVERT, DATÉ PLUTÔT QU'EFFACÉ : la passe de correction n'a été revue par personne
  # (règle §6 — 3 des 6 défauts majeurs de l'Epic 1 sont nés d'une passe de revue).
  3-6-assigner-recettes-et-nombre-de-personnes-aux-cases-du-menu: review
  epic-3-retrospective: optional

  # ── Epic 4 : La liste qui ne lâche jamais — hors-ligne, convergence & contrat ──
  epic-4: in-progress
  # Contextualisée le 2026-08-05. HUIT SONDES EXÉCUTÉES sur le stack local (PostgreSQL 17.6)
  # avant d'écrire une ligne de prescription — la story ne postule rien de ce qu'elle peut
  # mesurer. Les quatre qui changent la migration :
  #   · un `unique (…, unit)` ORDINAIRE laisse passer DEUX lignes dès que `unit` est nul,
  #     c'est-à-dire sur le cas le plus courant du produit. L'AC2 serait faux en silence.
  #     `nulls not distinct` (PG15+) le ferme, et il EXIGE un `create unique index` ;
  #   · sans `normalize(name, NFC)` dans la clé, « crème » en NFC et en NFD font DEUX lignes —
  #     le défaut exact que `lib/texte.ts` documente pour `unique(household_id, name)` ;
  #   · l'INSERT nu de `generate_grocery_list_from_menu` échoue en `23505` sur DEUX chemins une
  #     fois l'index posé (l'acheté qui survit au DELETE, et son `group by` sur `product_id`) ;
  #   · la vue `grocery_list_by_aisle` MONTRE un article tombstoné, et son corps `g.*` ne se
  #     rejoue pas : `create or replace` échoue (« cannot change name of view column »). Les
  #     colonnes neuves s'appendent en fin, mesuré.
  #
  # ✅ LES 4 DÉCISIONS TRANCHÉES par Florian le 2026-08-05, AVANT démarrage. La première
  # CONTRE la recommandation : la clé canonique PLIE AUSSI LES ACCENTS. Ce que ça change —
  #   · la migration installe `unaccent` (`with schema extensions`, sinon elle atterrit dans
  #     `public` — mesuré) et une enveloppe `strip_accents` déclarée `immutable` ;
  #   · ⚠️ le contournement que tout le monde connaît — « employer la forme à deux arguments,
  #     elle est IMMUTABLE » — est FAUX sur PG 17.6 : MESURÉ, les DEUX formes sont `STABLE`.
  #     Sans enveloppe, l'index est refusé ;
  #   · ⚠️ `immutable` y est une PROMESSE, pas un constat : le dictionnaire est un fichier sur
  #     disque. S'il bouge, l'index garde des clés périmées et une recherche peut manquer une
  #     ligne SANS erreur ni log. Contre-mesure écrite : `reindex` après toute montée majeure ;
  #   · mesuré sur cinq familles à unité égale — casse, accents, NFC/NFD, invisible, unité
  #     nulle deux fois : `23505` aux cinq. Et deux unités différentes font bien deux lignes.
  # D2, D3, D4 conformes aux prescriptions : fonction de génération DATÉE pour la 4.7 (non
  # touchée), index TOTAL (le tombstone garde sa clé, AD-3), vue amendée ici.
  #
  # ✅ Décision de Florian : la story se démontre INTÉGRALEMENT EN LOCAL. `SUPABASE_DB_URL`
  # n'est plus requis, et la requête de contrôle s'exécute sur le stack local — consignée
  # comme telle, pas comme une mesure de production (règle §1).
  #
  # ⚠️ ZÉRO test touche `grocery_list_items` aujourd'hui, et AUCUN code applicatif ne la lit
  # (mesuré). La story doit donc les DEUX familles d'AD-17 sur cette table.
  # ✅ IMPLÉMENTÉE le 2026-08-05 (statut `review`). Migration `20260805092611`, sept volets ;
  # 21 tests neufs (87/87 en isolation, contre 66 avant) ; `npm test` inchangé à 198/198, ce
  # qui est le résultat ATTENDU — cette story n'écrit aucun code applicatif.
  # ⚠️ ONZE PASSAGES AU BANC DES DENTS, et ils ont trouvé DEUX affirmations fausses de la
  # contextualisation, corrigées dans le code ET dans les commentaires :
  #   1. le test NFC n'avait AUCUNE dent — `unaccent` retire les diacritiques combinants tout
  #      seul, donc retirer `normalize(NFC)` de la clé laissait le test vert. Un test neuf
  #      (jamo Hangul vs syllabe précomposée) tient désormais la première opération de la clé ;
  #   2. le `with check` de `grocery_update` n'est PAS ce qui refuse un déplacement
  #      inter-foyers — c'est la politique SELECT. Le `with check` est gardé comme ceinture,
  #      et le fait qu'aucun test ne le fasse tomber est ÉCRIT plutôt que laissé croire.
  # ⛔ CE QUI RESTE OUVERT, DATÉ : `generate_grocery_list_from_menu` échoue en `23505` sur deux
  # chemins mesurés et fait toujours un DELETE dur → story 4.7 (décision de Florian). L'AC4 est
  # donc tenu POUR LES SURFACES, pas pour cette fonction `security definer`.
  # ✅ REVUE ADVERSARIALE FAITE le 2026-08-05 — trois couches parallèles (Blind Hunter, Edge Case
  # Hunter, Acceptance Auditor). 6 décisions tranchées par Florian, 16 correctifs appliqués,
  # 4 reports datés, 4 écartés. DEUX CONSTATS BLOQUANTS, tous deux mesurés :
  #   ⛔ L'AC4 ÉTAIT DÉFAIT. `generate_grocery_list_from_menu` est `security definer` avec
  #      `execute` accordé à PUBLIC : un membre ordinaire effaçait sa liste EN DUR, tombstones
  #      compris, par un simple `POST /rest/v1/rpc/` (mesuré : 2 lignes → 0). La prémisse qui
  #      couvrait le report — « aucun point d'appel » — était mesurée sur le source TypeScript et
  #      ne disait rien de la surface REST. Fermé par un HUITIÈME VOLET (`revoke execute`), sans
  #      toucher au corps de la fonction : D2 tient, la réparation reste la 4.7.
  #   ⛔ L'AC2 ÉTAIT FAUX pour 241 points de code invisibles. Trois « Creme fraiche »
  #      indiscernables à l'œil coexistaient dans le même foyer et la même unité (mesuré). Deux
  #      plages manquaient, établies PAR SONDE : U+180F — voisin immédiat de la plage déjà
  #      énumérée, ajouté par Unicode 14 — et U+E0100–U+E01EF. Regex étendue : 0 survivant.
  # ✅ TROIS CONTRAINTES NEUVES, posées pendant que la table est encore vide (décision de
  # Florian ; la fenêtre se referme à la 4.4) : `nom_borne` (sans elle la clé rendait `54000`,
  # une erreur btree intraduisible, et le défaut était INTERMITTENT selon l'entropie du nom),
  # `acteur_couple` (AD-9 : la provenance EST le couple) et `intention_bornee` (`intent_at` à
  # `'infinity'` gagnait définitivement tout arbitrage LWW futur).
  # ✅ Aussi corrigés : le trigger `updated_at` ne couvrait pas l'INSERT ; `nom_non_vide` et la
  # clé n'employaient PAS le même prédicat malgré deux commentaires l'affirmant ; la requête de
  # contrôle portait une regex simplifiée qui SOUS-DÉTECTAIT ; et `create extension if not
  # exists … with schema` est un no-op qui aurait tué la migration EN PLEIN DÉPLOIEMENT si
  # `unaccent` préexistait dans `public` (mesuré de bout en bout).
  # ⚠️ HUIT PASSAGES AU BANC DES DENTS, tous propres — et le banc a trouvé UNE AFFIRMATION FAUSSE
  # DANS LA CORRECTION ELLE-MÊME : `revoke … from anon, authenticated` ne retirait RIEN, l'entrée
  # `PUBLIC` de l'ACL survivant. Sans le contrôle, le correctif aurait été consigné « appliqué »
  # en ne fermant rien (règle §6 : la passe de correction doit être revue à son tour).
  # ✅ Portes après correction : check:migrations 16/14/2/0 · lint · typecheck · npm test 198/198
  # (inchangé, c'est l'attendu) · test:isolation 95/95, contre 87 avant la revue.
  # ⛔ FERMÉE AVEC DEUX CONDITIONS OUVERTES, DATÉES (règle §6 bis, « le fichier fait foi ») :
  #   1. la requête de contrôle n'a JAMAIS été exécutée sur la PRODUCTION — local uniquement ;
  #      sur le distant, que la table soit vide reste une DÉDUCTION ;
  #   2. `generate_grocery_list_from_menu` reste cassée (`23505` sur deux chemins) et fait
  #      toujours un DELETE dur → story 4.7. Plus aucune surface ne l'atteint, mais elle n'est
  #      pas réparée.
  # ⛔ Quatre reports de revue dans `deferred-work.md` : `actor_id` non attaché à l'appelant
  # (4.6), `deleted_at` sans borne (4.5/4.10), `unit` non normalisé (Epic 6), `strip_accents`
  # exposée en RPC anonyme (4.12). Plus deux dettes transversales : `recipe_ingredients_nom_non_vide`
  # garde le trou que la 4.1 vient de fermer, et la régénération des types n'est PAS reproductible
  # (deux CLI Supabase sur le poste, aucune version épinglée).
  4-1-modele-canonique-de-la-ligne-d-article-isolation-rls: done
  # Contextualisée le 2026-08-05. SEIZE MESURES exécutées sur le stack local (`fb7b5c4`), et
  # trois d'entre elles invalident le cadrage naïf :
  #   ⛔ LE COMPOSANT QUE SON CRITÈRE DIT DE « RÉUTILISER TEL QUEL » N'EXISTE PAS. L'AC2 renvoie
  #     au « composant carte-rayon de l'Epic 2, story 2.4 » — mesuré, `2-4` est en `backlog`.
  #     `app/rayons/ListeRayons.tsx` est l'éditeur CRUD du parcours, pas ce composant. Décision D1.
  #   ⛔ LE REGROUPEMENT NAÏF EST FAUX. `aisles.sort_order` n'a aucune contrainte d'unicité :
  #     deux rayons ex æquo font S'INTERCALER leurs articles (mesuré). Regrouper « par lignes
  #     consécutives » rendrait DEUX cartes portant le même rayon. Il faut grouper par CLÉ.
  #   ⛔ AUCUN ARTICLE N'A DE RAYON RÉSOLU en base — et pas pour la raison qu'on croit :
  #     `resolve_aisle_id` EST câblée, c'est `product_aisle_map` qui est VIDE (story 2.3, backlog).
  #     Sur une base réelle l'écran n'affiche qu'un groupe : il faut poser `aisle_id` à la main
  #     pour éprouver le regroupement.
  # ⚠️ ELLE EST LA PREMIÈRE LECTURE CLIENT-DIRECT DU DÉPÔT. Mesuré : les 20 appels du client
  # navigateur sont des écritures, et aucun des 8 `useEffect` ne fait d'`await` de données. Il n'y
  # a donc AUCUN motif à copier pour les états chargement/erreur/vide — c'est le vrai poids de la
  # story, et ce n'est écrit dans aucun de ses critères. Corollaire mesuré : un `throw` dans un
  # `useEffect` NE TRAVERSE PAS `app/error.tsx` — l'écran resterait sur son squelette, muet.
  # ⚠️ L'AC1 dit « plus via des Server Actions `force-dynamic` » : mesuré, il n'y en a AUCUNE.
  # Rien à démanteler — la formulation vise le prototype retiré à la story 1.1.
  # ✅ CONTRAINTE INVERSÉE : contrairement à toutes les stories récentes, celle-ci EST démontrable
  # sur la prévisualisation Vercel — la migration de la 4.1 est en production depuis le 2026-08-05.
  # ✅ D1 ET D2 TRANCHÉES PAR FLORIAN LE 2026-08-06 :
  #   ✅ D1 → LA STORY 2.4 construit le composant carte-rayon. La 4.2 le CONSOMME, elle ne l'écrit
  #     pas. ✅ **DÉBLOCAGE COMPLET le 2026-08-07** : la 2.4 est revue et `done`. (Le déblocage du
  #     2026-08-06 n'était que PARTIEL — elle était alors en `review`.)
  #     Le composant existe (`app/_lib/CarteRayon.tsx`) et son contrat est désormais EXPORTÉ sous
  #     `ProprietesCarteRayon` : ⚠️ **l'IMPORTER, ne pas le recopier.**
  #     ⚠️ DEUX AJUSTEMENTS DE CONTRAT ISSUS DE LA REVUE, déjà répercutés dans le fichier de la
  #     4.2 : `pris?: number | null` (`null` ne veut PAS dire « zéro pris ») et `children`
  #     OPTIONNEL — il était requis, ce qui faisait du rayon vide de l'AC2 une erreur de typage
  #     chez l'appelant (`TS2741`, mesuré).
  #     ⛔ UNE OBLIGATION QUE LE COMPOSANT NE PEUT PAS VÉRIFIER, ET QUI EST CELLE DE LA 4.2 :
  #     `pris`/`total` et `children` DOIVENT être dérivés du MÊME tableau, dans la même expression.
  #     L'AC3 de la 2.4 interdit à la carte de connaître le type des articles, donc un filtrage des
  #     enfants avec un compte sur la liste non filtrée ferait annoncer « 3 sur 5 pris » au-dessus
  #     de 4 lignes — et AUCUNE porte ne le verrait. Le correctif par le type (`compte?: {…}`) a
  #     été pesé à la revue et écarté par Florian : le contrat D2 reste gelé.
  #     ⚠️ La 4.2 reste la PREMIÈRE à voir la carte EN SITUATION — vraie liste, vraies données,
  #     sous un vrai `<h1>`. Le parcours de la 2.4 s'est fait sur une route jetable : il a tenu
  #     l'AC3 et trouvé deux défauts, mais il ne remplace pas l'écran réel (piège n°1 de la 2.4,
  #     défaut réel de la story 2.2).
  #   ✅ D2 → LA STORY 4.5 rend les articles achetés. La 4.2 reste une lecture PURE : aucune
  #     migration, `check:migrations` reste à 16/14/2/0, et `lib/supabase/types.ts` n'est pas
  #     régénéré. Conséquence visible : le ratio `n/total` des cartes vaut structurellement `0/n`
  #     jusqu'à la story 4.3 (rien ne coche avant elle). Entrée datée dans `deferred-work.md`.
  # ⚠️ TROIS DÉCISIONS RESTENT SUR LEURS DÉFAUTS PRESCRITS : D3 la forme de la lecture
  # client-direct (`useEffect` + squelette dans le composant), D4 « Ma liste » et la route
  # `/courses`, D5 le gros compteur livré ici plutôt qu'en 4.13.
  # ⚠️ Passe de validation en contexte neuf : elle a trouvé UN TROU (la story ne disait pas ce
  # qu'une ligne d'article affiche — sans l'unité, AD-7 rend deux « Lait » inexplicables) et
  # QUATRE affirmations fausses, toutes corrigées.
  4-2-lecture-client-direct-de-la-liste-groupee-par-rayon: done
  4-3-cocher-et-decocher-idempotent-et-reversible: done
  4-4-ajouter-un-article-avec-agregation-et-unites-fermees: done
  4-5-supprimer-archiver-les-achetes-vider-la-liste: done
  4-6-provenance-de-chaque-article: backlog
  4-7-generer-la-liste-depuis-le-menu-sans-rien-ecraser: backlog
  4-8-consulter-la-liste-hors-ligne: backlog
  4-9-ecrire-hors-ligne-via-une-outbox-rejouee: backlog
  4-10-convergence-lww-par-champ-entre-appareils: backlog
  4-11-propagation-temps-reel-entre-surfaces: backlog
  4-12-contrat-de-liste-stable-et-versionne: backlog
  4-13-coche-reelle-plancher-d-accessibilite-de-la-liste: backlog
  4-14-etats-hors-ligne-et-differe-a-l-ecran: backlog
  4-15-filet-de-verification-tests-d-isolation-de-convergence: backlog
  # Reçues de l'Epic 2 le 2026-07-29 (ex-2.5, 2.6, 2.7)
  4-16-resoudre-automatiquement-le-rayon-d-un-article-ajoute: backlog
  4-17-groupe-a-classer-toujours-visible: backlog
  4-18-correction-de-rayon-apprenante-en-silence: backlog
  epic-4-retrospective: optional

  # ── Epic 5 : Le dashboard de la cuisine ──
  epic-5: backlog
  5-1-identite-d-appareil-scopee-foyer-revocable: backlog
  5-2-appairer-le-dashboard-sans-login: backlog
  5-3-afficher-la-liste-lisible-a-un-metre-tuile-courses: backlog
  5-4-cocher-et-supprimer-depuis-le-dashboard: backlog
  5-5-reflet-temps-reel-sans-rechargement: backlog
  5-6-menu-du-jour-sur-le-dashboard: backlog
  5-7-gerer-les-appareils-depuis-l-ecran-profil: backlog
  epic-5-retrospective: optional

  # ── Epic 6 : Les surfaces mobiles & l'ajout à la voix ──
  epic-6: backlog
  6-1-pwa-installable-sur-iphone-et-android: backlog
  6-2-cible-de-partage-systeme: backlog
  6-3-ajout-par-dictee-partage-sur-telephone: backlog
  6-4-raccourci-ios-mains-libres-a-identite-d-appareil: backlog
  6-5-pont-google-keep-ingestion-unidirectionnelle-et-idempotente: backlog
  6-6-degradation-gracieuse-alerte-du-pont: backlog
  epic-6-retrospective: optional

  # ── Epic 7 : Le pilotage conversationnel (Claude / MCP) ──
  # 2026-08-05 — `backlog` → `in-progress` : première story de l'epic contextualisée.
  # ⚠️ L'EPIC EST PRIS HORS ORDRE. Les Epics 4, 5 et 6 sont entiers en `backlog`, et la 7.1
  # est la SEULE des quatre stories de cet epic qui n'a besoin ni de la liste (Epic 4) ni
  # d'un écran : elle ne pose que l'authentification et son périmètre. Livrer la 7.1 ne
  # débloque donc PAS l'Epic 7 — les 7.2, 7.3 et 7.4 restent derrière l'Epic 4.
  epic-7: in-progress
  # 2026-08-05 — Story 7.1 contextualisée : `backlog` → `ready-for-dev`.
  # ⛔ `ready-for-dev` dit que le CONTEXTE est complet, PAS que le chemin est tranché.
  #    QUATRE DÉCISIONS SONT OUVERTES dans le fichier de story, dont une structurante :
  #    le mécanisme d'identité lui-même (clé de signature importée vs serveur OAuth 2.1 de
  #    Supabase). Ne pas lancer `dev-story` avant qu'elles soient tranchées. C'est la même
  #    honnêteté de statut que la règle §6 bis de `project-context.md`, côté ouverture.
  # ⚠️ SES TROIS CRITÈRES CITENT DES STORIES QUI N'EXISTENT PAS : l'AC1 renvoie à la 5.1
  #    (identité d'appareil), l'AC3 à la 5.7 (révocation depuis le web) — toutes deux
  #    `backlog`, aucune table `device_credentials` dans les 15 migrations. Selon la
  #    Décision 1, la 7.1 ABSORBE la 5.1 (chemin A) ou la REND SANS OBJET pour cette
  #    surface (chemin B). Dans les deux cas l'entrée `5-1-…` ci-dessus devra être reprise.
  7-1-acces-conversationnel-scope-foyer-sans-contournement-de-l-isolation: ready-for-dev
  7-2-piloter-la-liste-depuis-claude: backlog
  7-3-consulter-et-faconner-recettes-menu-depuis-claude: backlog
  7-4-consulter-et-modifier-rayons-regles-depuis-claude: backlog
  epic-7-retrospective: optional

# ── Actions issues des rétrospectives ──
# Alimenté par bmad-retrospective. `status` : open | in-progress | done
action_items:
  # Bloquant avant l'Epic 2
  - epic: 1
    action: "Ouvrir un environnement de test Supabase (branche ou supabase start local) — NFR-5 n'est vérifiable par aucun test tant qu'il n'existe qu'un projet, qui est la production"
    owner: "Florian"
    status: done
  - epic: 1
    action: "Écrire les tests d'isolation à deux comptes (NFR-5) une fois l'environnement disponible"
    owner: "Amelia"
    status: done
  - epic: 1
    action: "Pousser 20260729094500_grant_table_privileges.sql — aucune migration n'accordait de privilège de table, la chaîne ne reproduisait pas la production (requête de contrôle en en-tête ; no-op attendu en prod)"
    owner: "Florian"
    status: done  # 2026-07-29 — `supabase migration list` rend local == remote sur les 7
  - epic: 1
    action: "Réviser la story 2.1 et son recouvrement avec la 2.2 — seed_default_aisles existe déjà et amorce 11 rayons ; aucun code applicatif ne lit les rayons"
    owner: "John + Winston"
    status: done  # 2026-07-29 — 4 décisions arbitrées par Florian, epics.md révisé
  # Ouverte par la révision de l'Epic 2
  - epic: 2
    action: "Trancher la distinction Florian / conjointe : aucune colonne de rôle sur profiles, RLS par foyer et non par membre. Retirée de la story 2.3, à décider avec son coût (schéma + RLS + 11 tests d'isolation à rejouer)"
    owner: "John + Florian"
    status: done  # 2026-07-30 — DÉCISION DE FLORIAN : le foyer est SYMÉTRIQUE, tous les
                  # membres ont les mêmes droits. C'est EXPERIENCE.md qui avait tort, pas le
                  # schéma. Coût réel mesuré du modèle promis : 13 politiques RLS sur 18 (la
                  # story en estimait 11), 20 tests à rejouer, et une famille de tests
                  # d'isolation à deux membres du MÊME foyer qui n'existe pas.
                  # Corrigé dans EXPERIENCE.md (8 passages + décision en tête), epics.md
                  # (AC de 5.7 et 7.4), epic-2-revision (AC3 de la 2.4), ARCHITECTURE-SPINE.
                  # Distinction désormais explicite : la SURFACE décide de ce qu'on y montre,
                  # la PERSONNE ne décide de rien.
  - epic: 1
    action: "Pousser 20260728152418_require_non_blank_household_name.sql (requête de contrôle en en-tête du fichier)"
    owner: "Florian"
    status: done
  # Avant de clore l'Epic 1 pour de bon
  - epic: 1
    action: "Relire les sept écrans dans les deux thèmes, /auth/bascule compris — 6/7 faits le 2026-07-29 (3 défauts trouvés et corrigés) ; /onboarding vu en SOMBRE le 2026-07-29 pendant le parcours de la story 2.1. Reste le thème clair. La CAUSE du blocage est trouvée : config.toml gardait site_url sur le port 3000 et les modèles d'email par défaut de Supabase, donc aucun compte ne pouvait naître en local — corrigé"
    owner: "Florian"
    status: in-progress
  - epic: 1
    action: "Nettoyer les trois comptes témoins abandonnés en production (+nc1, +nc2, +nc3) et leurs foyers"
    owner: "Florian"
    status: done  # 2026-07-29, tableau de bord Supabase
  # Règles d'équipe, à appliquer dès la première story de l'Epic 2
  - epic: 1
    action: "Ne consigner comme vérifié que ce qui a été exécuté, en citant la commande — une déduction s'écrit « déduit »"
    owner: "Amelia"
    status: done  # 2026-07-30 — déplacée dans project-context.md, le fichier que chaque
                  # session d'agent charge. Elle vivait dans next-steps.md, un document de
                  # planification que personne ne lit en implémentant : c'est pour ça qu'elle
                  # est restée ouverte deux epics et qu'une passe de revue l'a violée
  - epic: 1
    action: "Un commentaire explique un pourquoi, jamais un état de la base"
    owner: "Amelia"
    status: done  # 2026-07-30 — déplacée dans project-context.md, avec les quatre cas réels
                  # où un commentaire est devenu faux (dont deux écrits pendant une revue)
  - epic: 1
    action: "Revue adversariale par story plutôt qu'en fin d'epic (décision de Florian : environnement de test ET revue)"
    owner: "Amelia"
    status: done  # 2026-07-30 — déplacée dans project-context.md, ENRICHIE : la passe de
                  # correction doit être revue à son tour. Mesuré le 2026-07-29 — la première
                  # passe avait réparé la moitié d'un défaut et en avait introduit trois
  # Ouvertes par la revue adversariale de la story 2.1 (2026-07-29)
  - epic: 2
    action: "Rejouer le parcours à l'écran de /rayons après les correctifs de revue — deux régions de statut, focus au clavier, refus de l'icône multiple, message « Ce rayon n'existe plus. ». Sur le stack local, dans les deux thèmes. Aucun test ne couvre le JSX (NFR-10)"
    owner: "Florian"
    status: done  # 2026-07-29 — six gestes, deux thèmes, focus mesuré dans le DOM.
                  # Détail dans la story, § « Parcours à l'écran »
  - epic: 2
    action: "Contrôler dans Vercel que SUPABASE_DB_URL n'est déclarée que pour l'environnement Production — c'est le rôle postgres, il traverse la RLS, donc c'est la clé de tout NFR-5. Non vérifiable depuis le dépôt (non lié)"
    owner: "Florian"
    status: done  # 2026-07-30 — confirmé par Florian. Rapporté, non mesuré par la revue
  - epic: 2
    action: "Regarder la première exécution du job CI `isolation` sur un runner GitHub — `supabase start` en CI est un chemin neuf, comme l'était le script de déploiement. Le YAML est valide et les tests passent en local ; ça ne prouve pas le runner"
    owner: "Florian"
    status: done  # 2026-07-29 — 17/17 sur le runner, puis 20/20 avec les contraintes
