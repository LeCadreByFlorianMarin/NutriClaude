# Revue de code — Epic 1, passe 2/3 : couche métier & Server Actions

_Date : 2026-07-28 · 14 fichiers, 1441 lignes de diff · état de l'arbre après la passe 1_

**Périmètre.** `app/foyer/{actions,invitation,membres}.ts`, `app/foyer/{DisplayNameForm,InviteCard}.tsx`,
`app/onboarding/*`, `app/login/LoginForm.tsx`, `app/page.tsx`, `app/foyer/page.tsx`,
`app/_lib/garde.ts`, `lib/supabase/queries.ts`.

**Couches exécutées.** Blind Hunter, Edge Case Hunter, Acceptance Auditor, `/tdd`, `/clean-code`,
`/clean-architecture`. Aucune n'a échoué.

**Contexte donné aux six couches** — les deux migrations de la passe 1 sont **appliquées en production
depuis le 2026-07-28** : `profiles_update_own` gèle `household_id`, `redeem_household_invite` décrémente
atomiquement. Les constats correspondants n'ont donc pas été re-signalés ; en revanche, plusieurs
documents affirment toujours le contraire, et c'est devenu un constat en soi.

**Note de méthode.** Les constats marqués ✅ ont été reproduits par exécution ou par lecture du code
courant, pas déduits.

---

## Décisions requises

- [x] [Review][Decision] ✅ tranché — **`Profile already exists` est traité comme un succès — l'utilisateur perd ce qu'il a saisi et atterrit dans un foyer qu'il n'a pas demandé** — `app/onboarding/CreateHouseholdForm.tsx:51-61`, `app/onboarding/JoinHouseholdForm.tsx:73-80`. Le commentaire justifie ce silence par « deux soumissions concurrentes ». ✅ **C'est l'inverse** : dans les deux RPC, le `exists (select 1 from profiles …)` s'exécute à l'entrée, donc ce message signifie que le profil **existait déjà avant la requête**. Une vraie course passe ce contrôle dans les deux transactions et échoue sur `duplicate key value violates unique constraint "profiles_pkey"` — chaîne qui ne contient pas le motif cherché, et qui est donc rapportée comme un échec. Le cas réel est rendu comme échec, le cas non-concurrent comme succès. Scénario : un membre laisse un onglet `/onboarding` ouvert sur la tablette, tape « Chez les Marin » + « Flo », valide → renvoyé vers `/` qui affiche son **ancien** foyer, sans un mot. Aucun foyer « Chez les Marin » n'a été créé. **Décision :** que doit voir quelqu'un qui a déjà un foyer ?

- [x] [Review][Decision] ✅ tranché — **La prémisse « base gelée » est caduque, et elle justifiait plusieurs renoncements** — AR-MIGRATIONS est cité comme contrainte dure dans les stories 1.3 à 1.6 (« `git status --short supabase/migrations/` doit rester vide »), et sert d'argument à au moins trois défauts assumés. Deux migrations existent désormais. Conséquence immédiate : **l'AC3 de la story 1.5 est aujourd'hui réellement satisfaite** (« le compteur d'usages ne devient jamais négatif »), alors que la story et `deferred-work.md:72` affirment encore que la course « reste ouverte » et est « irréparable sans migration sur une base gelée ». **Décision :** réécrire la contrainte AR-MIGRATIONS, et rouvrir les renoncements qui s'y adossaient ?

- [x] [Review][Decision] ✅ tranché — **Aucun code d'invitation ne peut être révoqué, et « Créer un autre code » laisse croire le contraire** — `app/foyer/InviteCard.tsx:105-112`, `app/foyer/invitation.ts:27-36`. Chaque appui insère une ligne ; `invitationEnCours` n'affiche que la plus récente. Les précédents restent valables sept jours et cinq usages, **invisibles et irrévocables** — alors que la politique `invites_delete_own` existe dans le schéma (`initial_schema.sql:270`) et n'est exposée nulle part. Scénario : on partage un code avec quelqu'un, la relation tourne mal, on tape « Créer un autre code » en croyant révoquer. Rien n'est révoqué. **Décision :** exposer la suppression, avertir à l'écran, ou assumer.

- [x] [Review][Decision] ✅ tranché — **`/onboarding` rend le formulaire en état `inverifiable` alors que `/` et `/foyer` lèvent** — `app/onboarding/page.tsx:14-19` vs `app/_lib/garde.ts:41-49`. C'est un choix de la passe 1, et il ouvre le chemin du premier constat ci-dessus : un membre qui a déjà un foyer, victime d'un `getUser()` lent (3 s), reçoit l'écran de création. Le commentaire promet que « les deux formulaires échoueront proprement » — ils échouent en **succès**. **Décision :** uniformiser (lever partout), ou garder le rendu et fermer le chemin d'échec-en-succès.

- [x] [Review][Decision] ✅ tranché — **Le contrat plpgsql a déjà contraint une migration, sans être écrit nulle part** — l'en-tête de `20260727161200_guard_invite_use_count.sql` note « La distinction des trois refus est conservée […] l'interface s'appuie dessus ». J'ai dû réordonner les contrôles pour préserver ces libellés. Le contrat existe donc, il a déjà coûté du travail, et aucune machine ne peut le lire. **Décision :** `lib/foyer/erreurs.ts` + test qui épingle les chaînes telles qu'elles figurent dans la migration (30 lignes), en écrivant la fonction **tolérante `code` ou `message` dès le départ** — il n'y a pas de préproduction, et Vercel et Supabase se déploient séparément, donc migration et déploiement ne peuvent jamais être atomiques.

- [x] [Review][Decision] ✅ tranché — **`app/foyer/{invitation,membres}.ts` sont des lectures de table rangées dans le cercle le plus externe** — aucun contenu spécifique à une route, et le client construit en dur (`invitation.ts:20`, `membres.ts:20`). Le dépôt porte deux réponses opposées à la même question : `getMembership` prend son client en paramètre et vit dans `lib/`, ces deux-là non. Argument mécanique : `package.json:14` = `node --test "lib/**/*.test.ts"` — **le glob du lanceur EST la frontière testable/non-testable**, et les y laisser les en exclut sans que personne ne l'ait décidé. **Décision :** les déplacer vers `lib/foyer/` avec le client en paramètre ?

- [x] [Review][Decision] ✅ tranché — **La validité d'une invitation reste définie deux fois, avec deux horloges** — `app/foyer/invitation.ts:27-34` refait en JS (horloge Node) le prédicat que `redeem_household_invite` applique en `now()` Postgres. Une vue `household_invites_valides` en `security_invoker` porterait le prédicat une seule fois — le précédent existe déjà dans le schéma (`grocery_list_by_aisle`, `initial_schema.sql:216-226`), et la migration serait strictement additive. **Décision :** vue SQL, ou statu quo assumé et tracé ?

- [x] [Review][Decision] ✅ tranché — **La déconnexion livrée en passe 1 n'est couverte par aucun AC, et contredit la lettre d'AD-13** — AD-13 réduit les Server Actions à « callback magic-link, émission de jetons d'appareil et d'invitations ». Elle invalide aussi une case cochée de la story 1.6 : « `git diff … app/foyer/actions.ts …` → **vide** ». Constat de fond de la couche `/clean-architecture` : AD-13 classe par **analogie de vocabulaire** et non par contrainte — `generate_household_invite` est `security definer` et n'exige aucun secret serveur, il pourrait tourner depuis le navigateur. Ce que la Server Action achète réellement, c'est `revalidatePath`. **Décision :** reformuler AD-13 sur le critère de cause (« écriture dont la conséquence doit être visible dans un rendu serveur, ou qui exige un secret serveur ») et amender la story 1.6 ?

- [x] [Review][Decision] ✅ tranché — **Un prénom ou un nom de foyer vide reste possible et s'affiche « Salut . »** — les deux RPC ne font aucun `trim`, la colonne est `not null` mais accepte `''`, et `trim()` côté client ne retire ni `U+200B` ni les marques directionnelles. Aucun repli au rendu (`app/page.tsx:28`, `app/foyer/page.tsx:54`). **Décision :** contrainte `check (btrim(display_name) <> '')` en base (migration additive), repli à l'affichage, ou les deux ?

---

## Correctifs

- [x] [Review][Patch] ✅ traité — **`/login?error=__proto__` casse l'écran de connexion** [`app/login/LoginForm.tsx:11,87`] ✅ **reproduit.** `MESSAGES` est un objet littéral indexé par un paramètre d'URL : `MESSAGES["__proto__"]` rend `Object.prototype`, `MESSAGES["constructor"]` une fonction. Comme le résultat est *truthy*, le repli `?? MESSAGES["envoi-impossible"]` ne se déclenche pas, et `{message}` fait lever React (« Objects are not valid as a React child »). L'unique porte d'entrée du produit rend la frontière d'erreur au lieu du formulaire, pour une URL que n'importe qui peut distribuer. Correctif : `as const` + union de clés, ou `Object.hasOwn`.
- [x] [Review][Patch] ✅ traité — **Un code d'invitation créé avec succès est annoncé comme un échec, et chaque nouvelle tentative en crée un vrai de plus** [`app/foyer/actions.ts:46-52`] Le RPC a déjà committé la ligne ; si la relecture de confirmation échoue en transport, la fonction rend `{ ok: false }` **et saute `revalidatePath`**. L'`error` de la relecture est détruit par destructuration, rien n'est journalisé. Trois appuis = trois codes vivants à 7 jours et 5 usages, dont le membre croit qu'aucun n'existe, et qu'aucun écran ne montre.
- [x] [Review][Patch] ✅ traité — **`seDeconnecter` jette l'erreur de `signOut` et redirige quand même** [`app/foyer/actions.ts:76`] Sur échec de transport, `auth-js` sort avant `_removeSession()` : le cookie survit. `redirect("/login")` s'exécute, le proxy voit une session valide sur un écran d'entrée et renvoie vers `/`. Sur la tablette de cuisine — le scénario que le commentaire invoque pour justifier la fonction — l'utilisateur voit un clignotement et reste connecté, sans un mot. Aggravant : la fabrique employée (`createServerComponentClient`) **avale silencieusement** tout échec d'écriture de cookie, avec pour justification « le rafraîchissement est assuré par le proxy » — ce qui ne vaut pas pour une déconnexion, le proxy ne déconnectant jamais.
- [x] [Review][Patch] ✅ traité — **`genererInvitation` peut lever au lieu de rendre `ResultatInvitation` — exactement ce que son commentaire déclare avoir écarté** [`app/foyer/actions.ts:30` → `lib/supabase/queries.ts:83`] Le commentaire explique qu'on évite `requireProfile()` parce qu'elle lève `NEXT_REDIRECT`. Mais `appartenance()` → `getMembership()` lève un `Error` dès que la lecture de `profiles` échoue — ajouté dans la même passe. `InviteCard.tsx:38` n'a pas de `catch` : le rejet remonte à `app/error.tsx` et **remplace tout l'écran foyer**. L'échappatoire a été choisie contre une levée et atterrit sur une autre.
- [x] [Review][Patch] ✅ traité — **Trois lectures rendent leur échec comme une affirmation confiante et fausse** [`app/foyer/membres.ts:24-29`, `app/foyer/invitation.ts:27-36`, `app/page.tsx:16-20`, `app/foyer/page.tsx:21-25`] Les quatre destructurent `{ data }` et jettent `error`. ✅ `membresDuFoyer` ne peut **jamais** légitimement rendre `[]` : `profiles_select_own_household` porte `or id = auth.uid()`, et l'appelant vient de passer `requireProfile()`. Zéro ligne est donc un pur signal d'erreur — et `app/foyer/page.tsx:42` l'affiche comme « **Tu es seul ici pour l'instant.** » à un membre d'un foyer de quatre. Même mécanique pour le nom du foyer, qui devient « Chez toi » / « Ton foyer », donnant à croire que le foyer a été renommé.
- [x] [Review][Patch] ✅ traité — **Un code qui expire dans deux minutes annonce « Encore 1 jour », et la branche d'urgence est morte** [`app/foyer/invitation.ts:42`, `app/foyer/InviteCard.tsx:12`] ✅ `.gt("expires_at", maintenant)` garantit `restant > 0`, donc `Math.ceil(restant / 86_400_000) ≥ 1` : `if (jours <= 0) return "Il expire aujourd'hui."` est **inatteignable**, et `Math.max(0, …)` ne mord jamais. Scénario : on dicte le code au téléphone en disant « tu as jusqu'à demain » ; le soir même, « Ce code a expiré. »
- [x] [Review][Patch] ✅ traité — **`Not authenticated` n'est jamais traité, contre la microcopie explicite des stories 1.3 et 1.5** [`app/onboarding/JoinHouseholdForm.tsx:28-34`, `CreateHouseholdForm.tsx:51-61`] Les deux stories prescrivent noir sur blanc : « `Not authenticated` → *ne pas afficher* — **rediriger vers `/login`** ». Les deux formulaires le font tomber dans le repli générique. Un onglet laissé ouvert au-delà de la session affiche « Ça n'a pas marché. Réessaie dans un instant. » indéfiniment, et rien à l'écran ne mène jamais à la reconnexion — le seul geste qui marcherait est le seul que l'interface ne nomme pas.
- [x] [Review][Patch] ✅ traité — **Aucun `try/finally` : une levée fige les quatre formulaires sur « Un instant… » définitivement** [`LoginForm.tsx:62`, `DisplayNameForm.tsx:50`, `CreateHouseholdForm.tsx:46`, `JoinHouseholdForm.tsx:68`] `signInWithOtp` relève ce qui n'est pas une `AuthError`, `createNavigateurClient()` lève si l'env manque, un appel de Server Action peut rejeter en transport. Dans tous les cas `busy` n'est jamais remis à `false` : bouton désactivé, libellé « Un instant… », zone `role="status"` vide. Seule issue : recharger la page.
- [x] [Review][Patch] ✅ traité — **« Copié ! » reste affiché sous un code qui a changé** [`app/foyer/InviteCard.tsx:34,49,94`] Le `setTimeout` de 2,5 s n'est jamais annulé et `copie` n'est pas réinitialisé quand `code` change. Appuyer « Copier » puis « Créer un autre code » dans les 2,5 s affiche le nouveau code sous la mention « Copié ! » — alors que le presse-papier contient l'ancien. C'est celui-là qui part dans la conversation de famille.
- [x] [Review][Patch] ✅ traité — **Aucune borne de longueur sur les champs libres** [`DisplayNameForm.tsx:84-96`, `CreateHouseholdForm.tsx:87-111`, `JoinHouseholdForm.tsx:107-133`] ✅ `grep -rn "maxLength" app/ lib/` ne rend rien ; le `OnboardingForm.tsx` supprimé portait pourtant `maxLength={8}` sur le champ code, sans remplaçant. Un prénom de 5 000 caractères est accepté et rendu dans « Qui est là » et dans « Salut … » — pour tous les membres, sur tous les appareils, et **personne d'autre que son auteur ne peut le corriger**. Le nom du foyer, lui, n'est modifiable nulle part.
- [x] [Review][Patch] ✅ traité — **La normalisation du code ne retire que les espaces** [`app/onboarding/JoinHouseholdForm.tsx:52`] `generate_household_invite` n'émet que de l'hexadécimal (`0-9A-F`), donc `O→0` et `I→1` seraient sans perte. Aujourd'hui un code noté `388B-626A` sur le tableau de la cuisine — la forme sous laquelle huit caractères sont lisibles — part tel quel et rend « Ce code ne correspond à rien. Vérifie ce que tu as saisi. » : un conseil qui envoie relire un code correctement recopié.
- [x] [Review][Patch] ✅ traité — **Le commentaire central de `DisplayNameForm` décrit une politique qui n'existe plus** [`app/foyer/DisplayNameForm.tsx:39-49`] Il affirme que `profiles_update_own` n'a pas de `with check` et conclut « **La discipline est ici** » — c'est-à-dire : l'UI est la garde. La migration appliquée aujourd'hui a fermé le trou. Pire, la migration cite ce fichier comme preuve que son correctif est sûr : les deux documents se contredisent désormais. Un mainteneur de l'Epic 2 y lira qu'il doit compenser en TypeScript un trou qui n'existe plus.
- [x] [Review][Patch] ✅ traité — **Deux entrées de `deferred-work.md` affirment ouverts des trous fermés** [`deferred-work.md:72` (course sur `uses_remaining`), `:88` (absence de `with check`)] Les deux se concluent par « exige une migration sur une base gelée ». Les deux migrations sont appliquées.
- [x] [Review][Patch] ✅ traité — **La lecture du profil n'a pas de délai, alors que le commentaire annonce « la même garde que le proxy »** [`lib/supabase/queries.ts:56-76`] `withTimeout` couvre `getUser()` ; le `select` sur `profiles` juste en dessous est un `await` nu. Si l'auth répond mais que PostgREST sature, la garde de 3 s ne se déclenche jamais, `inverifiable` n'est jamais produit, et la requête pend jusqu'au délai de la plateforme — hors de `app/error.tsx`, hors de NFR-1, hors de tout message français.
- [x] [Review][Patch] ✅ traité — **Le glob de test ne couvre pas `app/`** [`package.json:14`] `node --test "lib/**/*.test.ts"`. ✅ vérifié : un fichier de test déposé sous `app/` n'échoue pas — **il n'existe pas**, et la CI reste verte. C'est le pire mode de défaillance possible pour un harnais.
- [x] [Review][Patch] ✅ traité — **`lib/supabase/queries.ts:8` importe sans extension, ce qui rend l'unité la plus testable de la couche non chargeable** [`lib/supabase/queries.ts:8`] `from "../auth/panne"` — ✅ `ERR_MODULE_NOT_FOUND` sous `node --test`. C'est le seul fichier de `lib/` qui enfreint la convention que `tsconfig.json` documente. ✅ Vérifié que l'ajout de `.ts` laisse `typecheck` **et** `build` au vert.
- [x] [Review][Patch] ✅ traité — **Basculer de mode pendant une soumission emmène l'utilisateur ailleurs, et efface ce qu'il a tapé** [`app/onboarding/OnboardingChoice.tsx:23-41`] Ni l'un ni l'autre des deux boutons n'est désactivé pendant qu'un formulaire enfant est occupé : la promesse en vol résout après démontage et appelle `router.replace("/")` depuis le formulaire que l'utilisateur venait d'ouvrir. Et le prénom déjà saisi disparaît au basculement, chaque enfant portant son propre état local.
- [x] [Review][Patch] ✅ traité — **Le presse-papier indisponible ne produit aucun retour** [`app/foyer/InviteCard.tsx:44-54`] Sur une origine non sécurisée — une tablette de cuisine en `http://192.168.x.x`, plausible pour ce produit — `navigator.clipboard` est `undefined` et le `TypeError` part dans un `catch` volontairement muet. L'utilisateur appuie sur « Copier » et il ne se passe strictement rien : ni confirmation, ni erreur, ni suggestion de sélectionner le code à la main.
- [x] [Review][Patch] ✅ traité — **Cinq tables `MESSAGES: Record<string, string>` et cinq fois le même repli** [`LoginForm.tsx:11,87` · `CreateHouseholdForm.tsx:11,69` · `JoinHouseholdForm.tsx:15,88` · `InviteCard.tsx:7,56` · `DisplayNameForm.tsx:8,101`] Le `Record<string, string>` élargit le type à toute chaîne, donc le compilateur ne peut plus attraper une clé fautive, et le repli à l'exécution compense un typage qu'on a soi-même désactivé. Cas concret : `DisplayNameForm` mélange succès et échecs dans la même table avec `echec` en repli — une clé de succès mal orthographiée afficherait « Ça n'a pas marché » sur un enregistrement réussi. C'est aussi la cause racine du crash `__proto__` ci-dessus.
- [x] [Review][Patch] ✅ traité — **L'état de soumission est recopié dans trois formulaires, et a déjà divergé** [`CreateHouseholdForm.tsx:21-58` · `JoinHouseholdForm.tsx:40-77` · `DisplayNameForm.tsx:23-66`] `DisplayNameForm:93` efface le message dès que l'utilisateur retouche le champ ; les deux autres non — on corrige son prénom et « Il manque ton prénom » reste affiché. La troisième copie a appris quelque chose que les deux premières ignorent.
- [x] [Review][Patch] ✅ traité — **Le test « déjà inscrit » est dupliqué et à double négation** [`CreateHouseholdForm.tsx:51-61`, `JoinHouseholdForm.tsx:73-80`] Un `if` imbriqué dont la branche utile est une négation et dont la branche significative est **vide**. Le lecteur doit remonter au commentaire pour comprendre qu'on tombe volontairement dans le vide.
- [x] [Review][Patch] ✅ traité — **`toErrorKey` et `versCle` : même rôle, deux noms** [`LoginForm.tsx:30`, `JoinHouseholdForm.tsx:28`] Le seul endroit où le mélange français/anglais coûte au lecteur : deux fonctions qui traduisent toutes deux une erreur fournisseur en clé interne, sans se ressembler.
- [x] [Review][Patch] ✅ traité — **La même requête `households` dans les deux pages, et quatre clients par rendu de `/foyer`** [`app/page.tsx:16-20`, `app/foyer/page.tsx:18-28`] Les deux pages délèguent leurs autres lectures à des fonctions nommées et plongent ici dans Supabase à la main — deux niveaux d'abstraction dans le même corps. Et `requireProfile`, la lecture `households`, `invitationEnCours` et `membresDuFoyer` construisent chacun leur client, en série alors qu'ils sont indépendants.
- [x] [Review][Patch] ✅ traité — **`86_400_000` en dur, `cle` et `bascule` mal nommés** [`invitation.ts:42`, `DisplayNameForm.tsx:24`, `OnboardingChoice.tsx:20`] `cle` porte à la fois un succès et deux échecs ; `bascule` nomme un comportement mais contient une chaîne de classes CSS.
- [x] [Review][Patch] ✅ traité — **Le commentaire de la relecture de confirmation ne décrit plus le code** [`app/foyer/actions.ts:39-50`] Il annonce deux raisons ; le `select` qui suit ne demande que `code` et ne sert qu'à un test de nullité. La première raison (lire durée et usages) est morte — c'est `invitationEnCours` qui s'en charge, au rendu.
- [x] [Review][Patch] ✅ traité — **La story 1.6 coche une vérification devenue fausse** [`1-6-…md`, Task 6] « `git diff app/foyer/InviteCard.tsx app/foyer/actions.ts app/foyer/invitation.ts` → **vide** » — `actions.ts` porte désormais `seDeconnecter`, et `page.tsx` une cinquième section hors de l'ordre imposé par la Task 1.

---

## Écarté comme bruit

- « `genererInvitation` écrase trois échecs distincts en `"echec"` » — vrai, mais c'est le même correctif que la table `MESSAGES` typée ; fusionné.
- « Trois allers-retours en série sur `/foyer` » — fusionné avec le correctif de duplication de requête.
- « `lib/dates.ts` supprimé prive le formatage de son foyer naturel » — observation juste, sans action avant l'Epic 5.
- « Aucun test sur la couche métier » — ce n'est pas un constat distinct : c'est la conséquence des deux correctifs de harnais (glob, extension) et des coutures d'architecture.
